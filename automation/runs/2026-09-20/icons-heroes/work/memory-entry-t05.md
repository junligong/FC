
### 2026-09-20 05:02–05:06（T05）— 首轮即成功 131/131（2m30s、零劣化、补采未触发）；建议集 36 → 38（真实进 6 出 4）、**缺陷修正值 44**；区间连续 25 轮持平；**主文件再归档 245 行**
- 预检 `node automation/browser-triage.mjs --json` **exit 0 / verdict `OK` / actions `[]`**（**未自愈**；checkDepsExit 0、`proxy: ready (Chrome (独立调试 profile))`、site-patterns futbin.com/worldfootball.net、chrome.port 9333、listener true、`/json/version` 200（Chrome/153.0.8010.52）、`/json/list` `[ ]`、proxy.health.connected true（source `debug-port-override`）、sessions 1、managedTabs 0、config `chrome`、generatedAt `2026-09-19T21:02:24.771Z`）。**通道健康**。
- **第 2 步 131/131 首轮即成功**（2m30s、17 批、**5 次宿主页重建**；进度数与成功数逐批一致、**补采轮未触发**、零 CDP 劣化），exit 0，`counts{total:131,ok:131,missing:0,minFloor:69000,maxCeiling:15000000}`、`errors []`、`ok=false` 0 张、`missingItems` 仅口径提示串。落 `hourly/2026-09-20T05.json`（`collectedAt 2026-09-19T21:04:59.979Z`）。**未写 `hourly-05-failed.json`**（T14/T15 全量 403、T16 部分失败均未复现）。
- ⚠ **本轮的前一轮是 T03，不存在 T04 快照**（本任务相位由 03:58 漂到 05:02 启动）⇒ `PREV='2026-09-20T03'`、`hPrev` 指向 T03；**不得假定「前一轮 = 上一小时」**，改脚本前先 `ls pricerange/hourly/` 取实际前轮。T03→T05 间隔约 1 小时 5 分，节奏正常。
- **T03 → T05 区间变动 0 张**（连续 **25** 轮持平：T21→T22 … T02→T03→本轮）；代表价变动 **88 张**、新转有效 2 / 有效转无效 6；任一平台有效 T03 118 → **T05 114（Console 115→112 / PC 77→76）**。
- 研究（脚本原样）：**投资建议 36 → 38（真实进 6 / 出 4）**。**判定链无回归的三重证明**：① `research-prev-t03.json`（跑第 3 步前快照，自判 36）rows 自洽复算前轮 = **36，差异 0/0**，`prevcheck` 基线核查「T03 未采集卡 0 张」；② 本轮 rows 复算 = 38、脚本自判 = 38、按 cardId 独立重算 = 38，三者 **0/0**；③ `verify` 内置「FC26 映射并发漂移」= **0 张**（T03 首次非 0 的 14 张属上游一次性换名，本轮前后一致故不再漂移，**缺陷本身仍在，见下**）。
  - **进 6 张全部 condA 转 true**（卡 / cardId / con / pc / rep / fc26Launch / 余量比）：巴雷西 `21524` 340,000·340,000 / 340,000 vs 351,000（**1.0324**）· 亨利 `21525` 3,299,000·4,000,000 / 4,000,000 vs 5,681,158（1.4203）· 卡恩 `21528` 870,000（pc 0）/ 870,000 vs 876,000（**1.0069，全场最临界，余量 0.7%**）· 范巴斯滕 `21539` 472,000·425,000 / 472,000 vs 494,873（1.0485）· 坎通纳 `21804` 1,999,000（pc 0）/ 1,999,000 vs 2,372,397（1.1868）· 佩蒂特 `21886` 600,000（pc 0）/ 600,000 vs 630,708（1.0512）。
  - **出 4 张（全部真实价驱动，无一为映射缺失；2 张双平台同时失效、1 张 PC 跃顶格、1 张上穿）**：穆勒 `21508` con 1,230,000→**0**（pc 亦 0，**双平台同时失效 ⇒ rep 归 0，condA 记 `null` 不适用**）· 卡洛斯·阿尔贝托·托雷斯 `21526` con 1,379,000→**0**（pc 亦 0，同上）· 琼斯 `21826` con 270,000 不变、pc 325,000→**335,000**（rep 325,000→**335,000** > 327,656，**T03 记录的 1.0082 余量已耗尽**，余量比 1.0082→0.9781）· 施魏因斯泰格 `21897` con 569,000→590,000 且 pc **0→15,000,000**（rep→15,000,000 跃至上沿，> 766,778）。**延续本日「PC 档持续滚动把代表价推过 FC26 开服价」的形态（T19/T20/T22/T23/T00/T01/T02 同型）。**
  - 保留卡余量最窄 Top12（`fc26Launch ÷ representative` 升序）：卡恩 1.0069 · 巴雷西 1.0324 · 哈维 `21523` 1.0415 · 范巴斯滕 1.0485 · 佩蒂特 1.0512 · 拉姆 `21823` 1.0731 · 布冯 `21519` 1.1243 · 埃辛 `21941` 1.1428 · 贝克汉姆 `21878` 1.1759 · 坎通纳 1.1868 · 哈吉 `21911` 1.1893 · 博格坎普 `21556` 1.2082。**condB 全场唯一**：斯科尔斯 `21864` 上沿 200,000 < 231,598（rep 0 未下穿 ⇒ 仅 condB）。
  - `counts{compared:131,advice:38,condA:37,condB:1,both:0,noFc26:26,noRange:0}`；`condA=null(不适用)=35`；`minValidPrice 1000`、`priceBasis listing-estimate`、`rangeCollectedAt 2026-09-19T21:04:59.979Z`。38 张建议卡 `nameZh` **齐备 38/38**。
- **★★ 上游 slug 换名缺陷仍在（T03 已立案，本轮只量化、不修）**：`noFc26` 仍 **26 张**（与 T03 同集），其中 T03 实测的 **13 张**为换名受害者（21488/21492/21495/21501/21531/21560/21567/21779/21807/21815/21829/21839/21877）+ 21526 属反向恢复（新 slug 可命中）。**本轮新增量化工具 `work/fc26bridge-t05.mjs`**（旧 slug 桥回 FC26，只读、不改产物，含连字符/空格两种书写变体，不做人名模糊匹配）：14 张全部桥回成功（0 张无法桥回），其中 **6 张修正后 condA 成立** ⇒ **修正建议集 = 38 + 6 = 44（condA 43 / condB 1）**，`noFc26` 修正 **26 → 13**（脚本内 `26−14=12` 的算法把反向恢复的 21526 也减了，准确值 13）。
  - 6 张新增命中（cardId / 台账名 → FC26 键 / con / pc / rep / fc26Launch / 余量比）：贝利 `21488`(`pele`→`arantes-nascimento`) 5,750,000·10,000,000 / 10,000,000 vs 11,613,469（1.1613）· 罗纳尔迪尼奥 `21501`(`ronaldinho`→`de-assis-moreira`) 4,380,000·9,000,000 / 9,000,000 vs 12,383,155（1.3759）· 罗伯托·卡洛斯 `21560`(`roberto-carlos-da-silva-rocha`→`da-silva-rocha`) 1,000,000·1,100,000 / 1,100,000 vs 1,689,610（1.5360）· 里瓦尔多 `21567`(`rivaldo`→`borba-ferreira`) 496,000·683,000 / 683,000 vs 842,905（1.2341）· 雅伊尔津霍 `21807`(`jairzinho`→`ventura-filho`) 2,900,000（pc 0）/ 2,900,000 vs 7,294,617（2.5154）· 马塞洛 `21839`(`marcelo`→`vieira-da-silva`) 730,000·875,000 / 875,000 vs 1,291,787（1.4763）。
  - 桥回后仍无建议 3 张（真实价驱动、非缺陷）：济科 `21531`(`zico`) rep 10,993,000 vs 6,831,947（0.6215）· 苏格拉底 `21829`(`socrates`) rep 1,030,000 vs 909,887（0.8834）· 西西 `21877`(`sissi`) rep 799,000 vs 289,071（0.3618）；另 5 张双平台价全 0 ⇒ rep=null、condA 不适用。
  - **处置不变（遵守契约边界）**：`build-icon-research.mjs` 的 slug 关联属契约语义（「两代卡按 FUTBIN slug 关联」）且契约禁止人名模糊匹配 ⇒ **本轮不单方面改写脚本**，照契约出产物、把缺陷与修正值写入报告与技能，等用户一次性裁定后全局同步。
  - **副产品（台账 `nameZh` 历史误标，写入方为每日任务、非本任务）**：本轮仍可见 21495 标「弗朗茨·贝肯鲍尔」（实为加林查、词库 `garrincha`）/ 21531 标「尤西比奥」（实为济科）/ 21560 标「儒尼尼奥」（实为罗伯托·卡洛斯）/ 21779 标「里瓦尔多」（实为卡卡）/ 21839 标「维埃拉」（实为马塞洛）/ 21807 标「加林查」（实为雅伊尔津霍）。**本任务不改台账，仅如实回报。**
- **★ 区间极值（并列张数已核）**：下沿最低 **69,000（21 张并列**，与 T03 完全同集：永里优季/加林查 21877/亚亚·图雷/坎贝尔/里贝里/维埃拉 21898/贝尔/道格拉斯/拉什/哈吉/肯佩斯/劳德鲁普/巴恩斯/赞布罗塔/加图索/卡福 21924/基耶利尼/阿什利·科尔/埃辛/范德萨/瓦拉内）· 上沿最高 **15,000,000（71 张并列**，即全局 ceiling：贝利/罗纳尔多/克鲁伊夫/21495/米娅·哈姆/罗纳尔迪尼奥/安格勒/布冯 …）。本轮为全量成功轮 ⇒ 与 T03 直接可比，**21/71 与 T03 完全一致，交叉自洽闭合**。自洽核验：有效平台价越界 [min,max] **0 张**、恰等于上沿者 **4 张**（T03 为 8，随平台价滚动而变，非异常）、区间宽度 0 者 0 张、有效平台价 Console 112 + PC 76 = 188（任一平台有效 114）。延续 T05(09-19)–T03 结构观察：ceiling 区分度低 ⇒ condB 对这 71 张**永不可能触发**，报 condB=1 时须连带说明。
- 产物校验（`verify-t05.mjs` / `extremes-t05.mjs` / `prevcheck-t05.mjs` / `detail-t05.mjs`，全部按 cardId）：`current.json` **2309 条**（icon 131 / 非传奇 2178，双平台键缺失 0）、`generatedAt 2026-09-19T21:05:00.002Z`、三源完好（futbin-popular 250 @`2026-09-19T20:02:26.498Z` / futbin-icon-detail 131 @`21:04:59.979Z` / daily-market 750 @`19:17:46.770Z`）；传奇 131/131 命中、`scope=card`·`source=futbin-icon-detail`·双平台键·`updatedAt=collectedAt` **全 0 违例**；**current↔T05 与 current↔daily 全 0 违例**（全量成功 ⇒ 无保留观测项）；`latest.json` 与 `hourly/2026-09-20T05.json` 一致；三个行情副本（主文件 / `reports/daily/D/assets/data/current.json` / `daily-merged/assets/data/current.json`）`generatedAt` 与条数（2309）一致；名单双向差集 0/0、research 行 id 去重 131/131。
- daily 快照 `players` 131/131 含 `priceRange`+`platforms.console/pc`；顶层 `priceRange{scope:"card",withRange:131,missing:0,minFloor:69000,maxCeiling:15000000,collectedAt:2026-09-19T21:04:59.979Z}`；`counts{total:131,valid:114,missing:17,platformValid:{console:112,pc:76}}`（`missing`=无有效价张数，非采集缺失）；快照内**无**任何日环比/累计涨跌字段；快照累计 **5 天**；历史 3 天 mtime **未变动**（09-16 08:20:15Z / 09-17 15:07:50Z / 09-18 15:35:14Z）。
- research **65,548 B**（八要素齐备：不构成投资 / 重演 / listing-estimate / 卡级 / 条件A ≥1000 有效性 / 无FC26对照 / 占位 / 不适用；38 张建议卡页内 **38/38**，判定用 `nameZh`/`name`，勿用 slug）；常驻底稿 `icons/reports/fc27-icon-live-research.html` 与当日页**逐字节一致**。icons-heroes **334,288 B**，五列表头齐备、`data-key=min/max` 各 1、**伪涨跌百分数 0**（3 处 `%` 全为 CSS 宽高）、1242 处「—」、头像 `球员头像 131/131`（`img`/`pimg` 均 179）、`data-card-id` **786**、运行时读 `assets/data/current.json`；**FC26 参考区（锚点偏移 261,054）img/pimg 均 0**（跨代不配头像红线保持）；按平台拆区间正则命中 1（口径说明句的**已知假阳性**）。
- **★ 快照校验和冲突（连续第 21 轮，契约既有结果，勿回滚）**：今日每日 icons-heroes 任务 `state.json`（status **`partial`**, completedAt `2026-09-19T19:52:32.006Z`, sha `66032ee67c1ed126…` / 334,370 B）；本轮第 4 步重渲染后产物 **334,288 B** 失配。属契约第 4 步既有设计结果；线上该栏目由 market-hourly 的重合并 + 重发布于下一整点接续（本任务自身不发布）。
- **本轮改动面（`find . -type f -newermt '2026-09-20 05:02:00'` 已核，恰 17 项、无多出文件）**：契约 **9 个产物**（`current.json`、`pricerange/{latest.json,hourly/2026-09-20T05.json}`、`daily/2026-09-20.json`、`research/fc26-vs-fc27-2026-09-20.json`、`icons/reports/fc27-icon-live-research.html`、`reports/daily/2026-09-20/{icons-heroes.html,market-icons-research.html,assets/data/current.json}`）+ 通道日志 `automation/runs/browser-channel-log/2026-09-20.jsonl` + `daily-merged/assets/data/current.json`（采集器共享模块 `src/current-market.mjs` 按设计分发，**不是发布**）+ `work/` 下 7 个辅助文件（verify/extremes/prevcheck/detail/fc26bridge/archive-memory 脚本 + `research-prev-t03.json`）。**必须用绝对时刻**（03:35–03:54 每日任务时段的大量写入会污染相对窗口）。
- **红线项全绿**：**未发布站点**（`automation/publish-status-2026-09-20.json` mtime 03:54，每日发布任务所写，本轮未动）；**未调用 run-state.mjs**（`automation/runs/2026-09-20/icons-heroes/owner.json` 为 03:44 每日任务所写）；**未渲染 market.html**（mtime 03:23，日任务所写）；`daily-merged/index.html` 仍 04:03（market-hourly T04 所写）未变；未动首页/版式、未改其他模块；未跑 `coordinate.mjs` / `verify-publication.mjs`；`hourly-05-failed.json 不存在`（成功轮应为不存在，符合）。
- **记忆维护（本轮已执行）**：主文件 **173,240 B / 356 行 → 47,962 B / 111 行**；09-19 的 **T07–T23（245 行）**无损移入 `memory-archive-2026-09-19.md`（该归档累至 172,637 B）；备份 `memory.md.bak-20260920T0508`；脚本 `work/archive-memory-t05.mjs`（含起止边界断言 + 行数守恒打印，可复用）。**主文件现只保留 09-20 的 T00–T05**。
- **复用经验（T05 新增）**：① **`PREV` 不能按「上一小时」推**——本轮实际前轮是 T03（T04 空档属相位漂移而非失败），改脚本前先 `ls pricerange/hourly/` 取实际前轮。② 四脚本 `cp` + **降序** sed（`T03→T05` 再 `T02→T03`、仅 `verify`/`extremes` 改 `const HH`、小写 `research-prev-t02.json→t03`）一次成功零误报；`grep -n "const D\|const HH\|const PREV\|research-prev-t"` 逐行复核。③ **新增可复用工具 `work/fc26bridge-t05.mjs`**：FC26 跨代关联缺陷的量化脚本（旧 slug 桥回 + 修正 advice / noFc26），下轮 `cp` 继承即可，只需确认换名清单未再变。④ **归档记忆用确定性 node 脚本**（先 `cp` 备份 → 断言起止边界 → 打印 356=34+245+77 行数守恒 → 校验 `###` 条目数），**不要用 heredoc**（T03 已记 heredoc 终止符可能不被识别的坑）。⑤ 本轮写入后主文件 48 KB，距 256 KB 上限充裕；**下次触发归档的阈值仍是 200 KB**。

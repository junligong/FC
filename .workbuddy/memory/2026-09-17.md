# 2026-09-17 工作日志

## FC·传奇英雄监控（icons-heroes）03:00 定时任务 —— failed
- runId `adb415d7-98d7-4c64-9ded-a186c31d1ed5`，03:00:30 启动 → 03:04 收口提交 `failed`。
- 浏览器通道正常（`check-deps.mjs` exit 0，Chrome :9222）。**根因是 FUTBIN 来源侧拦截**：`/players` 列表目录（`/27/players`、`?page=1`、`?rarity=icon`、`/27/players/evolutions`、`/26/players`）全部返回自有 403 页；同源对照 `/`（335 KB 正常）、`/27/popular`、`/27/player/21487/maradona`（11 ps + 11 pc 单元格）均可达 → 分路径反爬，非通道故障。
- 合规：未用禁止入口、未写虚假当日快照、未用 FC26/旧数据填充、未算涨跌。`reports/daily/2026-09-17/` 保持空（`icons-heroes.html` 未发布）。
- 证据：`automation/runs/2026-09-17/icons-heroes/evidence.json`（10 条来源含 URL/时间/结果，3 条对照组 ok）。

## 发现两处数据完整性缺陷（需后续单独立项修复）
1. **渲染器对缺失当日快照无标识**：`render-market-icons.mjs#renderIcons` 的 `today = snapshots.find(s => s.date === dateStr) || snapshots[snapshots.length - 1]`，`if (!today)` 永不触发 → 缺当日快照时页面静默展示上一份快照的价格，页头仍写「数据日期 D」。本轮因此**主动不发布**渲染件（移存 `automation/runs/2026-09-17/icons-heroes/work/icons-heroes.rendered-notShipped.html`）。
2. **英雄区块跨代混入 FC26 数据**：`render-icons-heroes.mjs` 的 `collectHeroes()` 递归扫描 `heroes/data/**`，读入 `heroes/data/prices/fc26/base-heroes.json`，把 93 张 **FC26** 英雄卡渲染为 FC27「英雄卡（Hero）台账」。该问题在 `reports/daily/2026-09-16/icons-heroes.html`（「英雄卡全量 93 张」）中已存在，线上页面自建版起即含。修复方向：限定 `fc27` 路径 + 校验 `game`/`launchDate` 字段。

## 已沉淀
- 更新 site-patterns：`~/.workbuddy/skills/web-access/references/site-patterns/futbin.com.md` —— 补充「403 是分路径的（/players 目录被拦，详情页可达）」「用同源对照判定被拦 vs 无数据」「被拦时退避到分钟级、不要秒级密集重试」「详情页不能替代列表页（无名单与 IS 估值列）」。

## FC·资讯采集（03:05 自动化，partial）
- 16/16 账号打开成功，71 条推文入库；媒体解析 71/71；图片落盘 61/61、0 热链；news-media 回归 14/14 绿。
- 修复 `shared/lib/report-assets.mjs`：`isCacheableXImage` 白名单补 `/tweet_video_thumb/`（动图封面），否则报告残留远程热链。
- CDP Proxy `/eval` 返回键是 `value` 不是 `result`，自建采集编排脚本已兼容。
- partial 原因：翻译服务 0 条译文，71 条标「待翻译」；产物 `reports/daily/2026-09-17/news.html`，证据 SHA-256 已存 run-state 快照。
## 市场监控 03:00（partial）
- runId 5ffd24f4；CDP 通道正常。/27/popular 与 /27/popular/evolutions 采集成功（250/500 卡，Console 193 / PC 197 有效价，FUTBIN 已开始滚动更新实价）；/27/players 三次间隔重试均 403 拦截页，价格分层与价格维度如实空状态，未用旧数据填充。产物 market.html + market-scan.html 已渲染，evidence + run-state 快照已提交（SHA b56ed319…）。
- 新坑：evidence openedAt 必须是本轮实际打开时间（+08:00 写对日期），否则 finish 以「复用旧证据」拒收；market.json 若漏 overview 键概览会全空。详见 automation 记忆文件。

## FC·足球日报（03:30 调度执行）
- 终态 partial（runId 6faaafbd…），verify-football-boards 退出码 0；降级原因=证据含缺失项时 run-state 自动 success→partial（run-state.mjs:60，设计行为）。
- 三榜 9 联赛块 + 新闻 14 条，4 个并行子代理经 CDP 采集，全部打开原文核实。
- 新经验：证据 sources[].openedAt 必须为可解析 ISO 时间且 ≥ startedAt；模板拼接须校验 div 配平。详见 automation memory d39303af。

## FC·进化专栏（evolution）2026-09-17 03:44–03:47 · partial（成功采集）
- 通道正常：`check-deps.mjs` exit 0，CDP 直连用户日常 Chrome。三个来源页均在本轮实际打开并复核。
- 产物：`reports/daily/2026-09-17/evolution.html`（280,470 B，徽标 PARTIAL）、`automation/runs/2026-09-17/evolution/evolution.json`（500 张进化卡 + 14 条进化路线）、`evidence.json`；快照 `.../evolution/report.html`，SHA-256 `1f7fc5d7817b15d40043d18342a1cfa7c30590471f5f03011f874a7ba5ca0da5`。
- 数据量级：`/27/popular/evolutions` 渲染 500 个卡片节点（无分页，疑似服务端截断）；`/27/evolutions` 14 条路径（Evolutions 8 / Pathway 4 / Training Camp 2，全部 FREE、全部 REPEATABLE）；`/27/evolutions/expired` = 0。路径数较 09-16 的 5 条新增 9 条。
- **去重口径更正**：同一球员的不同卡版本是独立条目（URL 带版本后缀，估值/热度各异），500 个 URL 全部唯一 → 按 `url + 进化名称` 去重，不再按球员名（09-16 按名去重把 500 条压成 449 条，属丢失）。
- **run-state 两个硬约束**（今日二次踩中，已固化理解）：① `evidence.missing` 非空时 `finish success` 会被降级为 `partial`（run-state.mjs:60）→ 直接提交 partial 并同步 evolution.json 的 status，避免报告徽标与权威状态不一致；② `evidence.sources[].openedAt` 必须 ≥ 本轮 `startedAt`，否则报「来源记录不是本轮实际打开」→ 用 `begin --rerun` 重跑时**必须在本轮内重新打开来源页**，不能沿用上一轮的 openedAt。
- 另一坑：`begin --rerun` 会把 `automation/runs/D/<module>/` 下内容（含 `work/`、`evidence.json`、`report.html`）整体 rename 进 `attempts/<旧runId>/`，重跑前需从 attempts 拷回工作脚本。
- 可复用采集链路（放在 `automation/runs/2026-09-17/evolution/work/`，下次可直接拷）：`extract-cards.js`（榜单卡，CDP eval）→ `extract-evolutions.js`（总览 14 条路径，`div.evolutions-overview-wrapper`）→ `build-json.mjs`（装配 + 去重 + 路线）→ `render-evolution.mjs`。
- 站点经验已回写 `~/.workbuddy/skills/web-access/references/site-patterns/futbin.com.md`（进化页路由与结构、卡版本去重、500 条上限存疑）。

## FC·抖音素材（06:00 自动化，站点截图 + 讲解稿）
- 产物 `deliverables/douyin/2026-09-17/`：`shots/`（7 张原图 3240 宽 + `preview/` 1080 宽 + `manifest.json`）、`讲解稿.md`、`口播稿.txt`。截图 7/8 栏目成功；`06-传奇英雄监控` 记 `missing_subtab`（上游 icons-heroes failed，站点当日只挂「传奇卡研究」一个子标签），未用旧数据填充，证据写入 manifest 的 `notes.legend_mo_missing`。
- 尺寸：00 今日总览 3240×2800（截断，真实 132871）、01 足球动态 3240×2477（完整）、02 资讯 3240×14000（截断，24520）、03 市场概览 3240×8048（完整）、04 市场扫描 3240×14000（截断，30020）、05 进化 3240×14000（截断，32219）、07 传奇卡研究 3240×14000（截断，13138）。
- **修 `apps/douyin/capture-shots.py` 真 bug**：从未创建输出目录 `deliverables/douyin/<日期>/shots/`，新日期首跑会在 `crop.save()` 处 8 个栏目全 error + manifest 写不进。已在 `tmpdir` 之后补 `os.makedirs(outdir, exist_ok=True)`。技能 `fc-daily-douyin-video` 第 10 节已补该坑与 `missing_subtab` 处置。
- 讲解稿：成片 55s / 口播 43s = 78.2%（逐段自证全部落在段长内），8 段结构。
- 当日发现三处**渲染侧口径瑕疵**（已写进讲解稿「口径与合规」第 4 条 + 技能第 8.5 节，未改站点产物）：① 首页「传奇/英雄专栏」徽标显示「当日已收录」，实际监控子栏目缺失（疑 `merge_daily_report.mjs:176` 在 legendSubs[0]=research 时固定 `ok`）；② `market-report.mjs:344` 静态提示仍写「开服前两个平台价均为 0」，与当日 `partial-live` 数据矛盾；③ `football.html` 的 `<title>` 仍是「2026年9月1日」。
- 另注：`01-足球动态.png` 停在默认「新闻」子标签（英超 4 条），积分榜/射手榜/助攻榜未入图——截图脚本目前不切报表内子标签，需要时可加 `--only` 之外的内层 tab 支持。

## 09:39-10:20 用户指令：FC26 口径修正 + 任务体检 + 修复失败任务
1. **渲染器修正**（apps/market/engine/scripts/）：`render-icons-heroes.mjs` 英雄数据按代际分流——只有 fc27 代际进台账，fc26 目录数据单独渲染为「FC26 参考对比」折叠区（明确标注非 FC27 监控口径）；`render-market-icons.mjs` 修复当日无快照静默回退最近快照的问题：`todayIsCurrent=false` 时显示 `STALE · 本日无新采集` 红色徽标 + sub 行标注快照日期 + 缺失说明，stat 卡标签改为「最近快照(日期)有效价卡数」。回归 36/36 绿。
2. **icons-heroes 重跑成功**（runId 213a542c，partial）：昨日 /players 全路径 403 具时限性，09:48 重试即解除。经验：`page` 参数在会话建立后有效（每页 30 行评分降序）；`?version=base_icon/heroes` 直接导航返回空表不可用；`td.table-name` 版本标签（Icon/Base Heroes）最可靠。翻页 1-12 共 360 行：131 张台账传奇全部命中（0 缺失）+ 50 张 Base Heroes + 45 Normal + 6 TOTW。快照 `daily/2026-09-17.json`（Console 5 / PC 1 张有效平台价，开服前滚动更新中）；base-heroes.json（fc27）首次落库。CDP 代理对含 `?.` 的长 eval 偶发返回空对象，用 function+显式判空绕过。
3. **契约完善**：prompts/icons-heroes.md 新增「FC26 数据口径」「来源 403 的处置策略」两节，修正 page 参数表述；task-definitions.json bootstrapPrompt 同步；futbin.com.md 站点经验更新。
4. **重新合并发布**：coordinate 同日重开 merge=success（icons-heroes 转 partial）→ 站点发布 → 根路径边缘缓存 9 分钟未收敛，重部署一次后 90 秒收敛 → verify 四检全过，SHA `8d1c0e41…`，12,027,110 B。

## 10:21-11:02 用户指令：市场扫描球员中文翻译 + FC27 资讯翻译成中文（并加到自动化任务中）
### 1) 新闻翻译链路修复（根因 → 新通道）
- **根因**：`generate_report.mjs` 的 `translateTweet()` 依赖已废弃的 DuMate 千帆代理（`DUMATE_QIANFAN_PROXY`）或 `apps/news/.api_key`——迁移到 WorkBuddy 后两者都不存在（`ls apps/news/.api_key` → No such file），必然全部返回空 → 当日 71 条全部「待翻译」。
- **新通道**：执行 AI 把逐条译文写入 `apps/news/data/translations-D.json`（键 = 推文 ID）。`generate_report.mjs` 三处接入：① `preTranslationOf()` 优先取译文文件；② 翻译循环命中即 `continue`；③ 同日重跑对旧卡片统一补齐翻译。本轮 71/71 命中，`待翻译` 计数 0。
- 契约落点：`prompts/news.md` 新增「中文翻译（必做，2026-09-17 起：走译文文件通道）」整节；`apps/news/AGENTS.md` 同步；`task-definitions.json` + WorkBuddy 任务「FC·资讯采集」30296b95 提示词同步。

### 2) 市场/进化译名链路（新建，可复用）
- **新建** `apps/market/engine/scripts/apply-market-name-zh.mjs`：合并 6 个译名来源（增量词库 → 历史补充表 → Wikidata 库 → FC26 首月数据 → 传奇台账 → FC27 英雄卡），按 **URL 全名 slug → 姓名 slug** 匹配，就地写 `nameZh`，未命中清单写 `automation/runs/D/market/work/missing-name-zh.json`。注入点位覆盖 `players` / `popular.value.items` / `popular.evolutions` / `overview.evolutions` / `overview.priceTiers[].items` / `priceDimensions[].items`。
- **新建** 词库 `apps/market/engine/data/players/name-zh-supplement-fc27.json`（593 条 `mappings`，只增不改）。
- **修 3 个缺陷**：① `slugOfUrl` 原正则 `/\/player\/\d+\//` 匹配不了带版本后缀的球员 URL（`/player/1272_8/pierre-emerick-aubameyang`，进化榜常见）→ 进化条目全部退化成按姓氏匹配、重名风险高；放宽 id 段为「任意非斜杠字符」后 419/500 → 498/500。② 3 行采集时 `name` 为 null → 新增「由 URL slug 回填英文显示名」兜底。③ 原词库把 `yue-tze-nam` / `wang-kit-tsui` 存成**空字符串占位**，被「先到先得」逻辑视为已存在导致永久失配 → 经公开资料核实为**茹子楠**（Yue Tze-Nam）/ **徐宏杰**（Wang-Kit Tsui）并清理空值占位。
- **覆盖率**：market players 750/750、evolution 500/500、未命中 0。
- **渲染侧**（不手改 HTML）：`render-market-overview.mjs` / `render-evolution.mjs` 补 `<span class="zh">` + `.c-name .zh` 样式；`render-icons-heroes.mjs` 英雄台账统一为「中文名 + `<span class="en">`英文原名」。
- 契约落点：根 `AGENTS.md` 新增「中文译名强制规则」整节；`prompts/market.md`、`prompts/evolution.md`、`apps/market/AGENTS.md`、`task-definitions.json` + WorkBuddy 任务「FC·市场监控」b195b907 / 「FC·进化专栏」f366055d 全部同步。

### 3) 顺带修掉的既有事实性错误与缺陷
- 根 `AGENTS.md` 的 `page` 参数表述（旧写「被服务端忽略」，实际会话建立后有效）与「开服前两平台价均为 0」旧结论（已被 `partial-live` 推翻）已更正。
- **传奇/英雄台账 15 条英文名 UTF-8 乱码**：`fc27-icons-playstyles.json` 中把 UTF-8 字节按 latin-1 解码（nazário→nazÃ¡rio、müller→mÃ¼ller、puskás→puskÃ¡s），已按 latin1→utf8 反解修复，报告内 `Ã/Â` 残留 0。
- **快照一致性坑（重要）**：`icons-heroes.html` 线下已被中文名改写但快照仍是英文版，`coordinate.mjs` 对可选面板**优先用 sha 匹配的快照**，不重提就会静默回退旧版。

### 4) 重提快照 + 重新合并发布
- 四模块 `begin --rerun` → **本轮真实重开来源页**（契约要求 openedAt ≥ 本轮 startedAt）→ 重渲染 → `finish partial`：
  news `492e3d7b`(123,397 B) / market `08a0f112`(28,229 B) / evolution `f1bb9f35`(298,327 B) / icons-heroes `0b22fe34`(194,410 B)。
- 来源核验：FUTBIN 首访 `/27/players` 命中 Cloudflare 挑战页，等约 30 秒自行通过；`page=2/8/12` 各 30 行（27/19/30 行含 Icon 标签）；X 16 个账号逐个重开可达。
- 合并发布：merge=success、failedPanels=0、index sha `83f8c016…`（12,118,761 B）→ sites 发布 → `verify-publication.mjs` **exit 0**，四检全过，**本次根路径无边缘缓存滞后**。
- 回归：`node --test automation/{execution,regression,verify-publication,news-media}.test.mjs` → 36/36 通过。

## 11:49-12:10 用户指令：传奇最低/最高价 + 当前价、传奇研究（FC26 开服价对照）、每小时子任务

### 1) 新发现（实机核验，已写入 futbin.com 站点经验与 AGENTS.md）
- **FUTBIN 详情页有 `Price Range: 最低 - 最高`**（服务端渲染，`div.price-box-full-width` → `Price Updated: N mins ago` + 区间值），并带 `https://www.futbin.com/27/priceranges` 变更日志页（列：Old/New Min、Old/New Max、Updated on；`?version=` 等筛选参数无效）。
- **区间是卡级字段，不是平台级**：同一张卡的 `platform-ps-only` 与 `platform-pc-only` 价格盒渲染出**完全相同**的区间值（20 张卡批量比对，差异数 0）。故只落一份 min/max 并标 `scope:"card"`，页面切平台时区间列不变；平台差异只在「当前价」列。
- **限流坑**：详情页并发 5 即返回 **HTTP 429**。必须串行 + 批间隔 + 退避重试。131 张全量约 4–6 分钟。
- 列表页表头只有每平台一个 Price 列，**没有** min/max；min/max 只在详情页与 priceranges 日志页。

### 2) 新建（可复用）
- `apps/market/engine/scripts/extract-icon-priceranges.js`：浏览器侧 IIFE，注入 CDP `/eval`，逐卡 fetch + DOMParser 解析区间/双平台当前价/估值；串行 + 429/5xx 退避。
- `apps/market/engine/scripts/collect-icon-priceranges.mjs`：Node 驱动（自建宿主页 `futbin.com/robots.txt`，用完自关；**不复用**用户标签页避免后台冻结），落库 `pricerange/latest.json` + `pricerange/hourly/<D>T<HH>.json`（原子写）。**`--limit` 试跑强制写 `dryrun/`**，防止试跑覆盖生产全量（本次踩过一次：4 张试跑覆盖了 131 张的 latest.json，已加防呆）。
- `apps/market/engine/scripts/build-icon-research.mjs`：按 slug 关联 FC26 开服价（`fc26/base-icons.json` 的 `prices.cross` 首日价，2025-09-18）与 FC27 当前价/最高价，输出 `reports/daily/D/market-icons-research.html` + 常驻底稿 `icons/reports/fc27-icon-live-research.html` + 结构化结果 `icons/data/research/fc26-vs-fc27-D.json`。
- 本日数据：区间覆盖 **131/131**（下沿最低 69,000 / 上沿最高 15,000,000）；FC26 可对照 117 张、无对照 14 张；投资建议命中 **2 张**（条件A 1 / 条件B 1）。

### 3) 关键口径决策（否则结论会错）
- **条件 A「FC26 开服价 > FC27 当前价」必须带有效性判据（当前价 ≥1000 才参与）**。开服前当前价普遍为 0（尚无挂单的占位值），不加判据则「FC26 开服价 > 0」对每张卡都成立 → 115 张全被判成投资建议，属错误结论。无有效价的卡记「不适用」。
- 条件 B（对照区间最高价）是当前唯一有效对照。信号强度：B 比 A 稳健（A 对照的是单一刊例价，易被个别低价挂单放大）。

### 4) 改动与接线
- `record-icons-daily.mjs`：合并 `pricerange/latest.json` → 逐卡 `priceRange{min,max}` + 顶层 `priceRange` 覆盖统计；区间文件缺失不构成失败。
- `render-market-icons.mjs`：台账新增「最低价」「最高价」两列（可排序，`val()` 改为按 data-key 泛化取当前平台份）+ 顶部覆盖统计卡 + 口径说明 + 缺失项提示；「今日价」列改名为「当前价」。
- `render-icons-heroes.mjs` 未改（英雄 FC26 分流与 STALE 徽标已由 09:39 那轮修好）。
- `merge_daily_report.mjs#buildIconResearchPanel` candidates 新增常驻底稿 `fc27-icon-live-research.html`（放在静态预测底稿 `fc27-icon-analysis.html` 之前，保证往期日期也能看到实时研究）；**未覆盖**该静态底稿（内容独立，报告内已注明两者互为补充）。

### 5) 每小时子任务
- 新建契约 `automation/prompts/icons-pricerange-hourly.md`；注册进 `automation/task-definitions.json` + `shared/config/project.json`（order 7，schedule hourly）+ `prompts/README.md` + 根 AGENTS.md 任务表与新增整节。
- WorkBuddy 自动化已创建：id `7123b0fe-8129-4570-95ce-8e96834f265e`，`FREQ=HOURLY;INTERVAL=1`（**HOURLY 规则不支持指定分钟**，无法避开 03:00/03:05，已如实说明）。
- **该任务刻意不使用 `run-state.mjs`**：其 `begin` 用每日排他锁（`owner.json` 的 `wx` 创建），同一天第 2 次会被拒 → 套用会让 02:00 之后的每小时运行全部失败。运行记录以 `pricerange/hourly/` 逐小时快照为准。此约束已写进契约与 AGENTS.md。
- 回归：36/36 通过。

### 6) 未做（需用户明确指令）
- **未发布站点**。`reports/daily/2026-09-17/icons-heroes.html` 已含新三列，但线上要看到需重提 icons-heroes 的 run-state 快照（`begin --rerun`，当日已有终态运行）+ 重新合并发布。属对外动作，等用户明确要求。

## 13:0x 传奇价格区间小时任务（首次实跑）—— 关键踩坑已固化

- 4 步顺序全绿：采集 **131/131**（区间 69,000 – 15,000,000，listing-estimate，errors 0）→ 研究对照 131 张、投资建议 **2 张**（条件A 1 加林查 / 条件B 1 斯科尔斯）→ 快照 131/131 区间合并 → 监控页三列重渲染。未发布站点。
- **新发现的失败模式（务必记住）**：首轮采集前 4 批（32 张）正常，之后 99 张**全部**报 `CDP 命令超时: Runtime.evaluate` / `Unexpected end of JSON input`。
  这是 **CDP 代理传输层劣化，与 429 / Cloudflare 无关**；脚本内置的「补采一轮」在通道持续劣化时也拿 0 张。
  处置：`check-deps` 确认 exit 0 → `--limit 8` 试跑（只写 `pricerange/dryrun/`，脚本有防呆不会覆盖生产）验证通道 → 重跑全量（本次 3m11s 恢复 131/131）。
- `collect-icon-priceranges.mjs` 只要采到 ≥1 张就会写 `latest.json`（仅 0 张才 exit 1），所以**部分失败会用残缺结果覆盖全量 latest**；但逐小时快照保留完整版（T12 仍是 131/131），不丢数据。判定完整性请比对上一小时快照。
- 修正 `record-icons-daily.mjs:203` 写死的「开服前两平台均为 0，属预期」——与当日实测 Console 5 / PC 1 矛盾，违反「不要照抄均为 0」；已改为按 `platformValidCount` 动态措辞。`regression.test.mjs` 13/13 通过。
- **待决**：每日快照的 `platforms.console/pc` 当前价只来自 03:00 列表页 `base-icons.json`，不含本任务详情页的更新价（详情页 20 张有效 vs 快照 6 张），故监控页「当前价」列比研究里用的当前价旧。是否合并需用户拍板（会动 03:00 每日任务快照语义）。

## 14:1x 传奇价格区间小时任务（第 2 次实跑）

- 4 步顺序全绿：采集 **131/131**（4m19s，errors 0，区间 69,000 VARANE – 15,000,000 PELÉ，与 T13 一致）→ 研究投资建议 **2 张**（条件A 1 加林查 console 129,077 / 条件B 1 斯科尔斯 上沿 200,000）→ 快照合并 131/131 区间 → 监控页重渲染。未发布站点。
- **本轮回合通道完全健康**：17 批无一超时，无需 dryrun 探测与重跑 → 反向印证 13:0x 的 99 张全失是**偶发 CDP 传输层劣化**，非名单或页面结构问题。该故障模式仍保留在处置预案里。
- **口径判定适用性（新记录，防误读）**：条件B 可判定 117 张；**条件A 仅 5 张可判定**（需 FC26 对照 + 有效 FC27 当前价）= jones / shevchenko / lima do amor / laudrup / essien。其余 112 张有 FC26 对照但当前价无效，**属口径设计不适用，不算采集缺失**。
- 实时平台价滚动：T13 有价 20 张 → T14 21 张（Console 有效 17 / PC 有效 6）；`priceBasis` 仍 listing-estimate，未算日环比/累计涨跌。
- **字段位置备忘**：`pricerange/*.json` 逐卡用 `current.console` / `current.pc` + `currentValid.*`，**不是** `platforms.*`（后者是 `daily/<D>.json` 结构）；键名用错会把 21 张有价误判为 0 张。`missingItems` 可能只放口径提示串，判缺失要看 `counts.missing` 与 `cards[].ok`。
- 产物质检：研究页含「不构成投资或交易建议」+「历史开服价不代表 FC27 会重演」；监控页最低价/最高价/当前价三列齐备；无按平台拆分区间的痕迹。


## 15:2x 传奇价格区间小时任务（第 3 次实跑，T15）

- 4 步顺序全绿：采集 **131/131**（3m52s，errors []、missing 0，区间 69,000 VARANE – 15,000,000 PELÉ/`nazario-de-lima`）→ 研究投资建议 **2 张**（条件A 1 加林查 289,071 > 129,077；条件B 1 斯科尔斯 231,598 > 上沿 200,000；双命中 0）→ 快照合并 131/131 区间 → 监控页重渲染。未发布站点，未调 run-state。
- 通道再次完全健康（17 批零超时），与 T14 共同印证 13:0x 属**偶发 CDP 传输层劣化**。
- **T15 相对 T14 逐卡区间变化数 = 0**（131 张最高/最低价全部持平，minFloor/maxCeiling 均未变），说明 FUTBIN 区间本轮未滚动；实时平台价仍在小幅增加（T15 Console 20 / PC 6，快照侧仍为 03:00 的 Console 5 / PC 1）。`priceBasis` 恒为 listing-estimate，不计算日环比/累计涨跌。
- 新增踩坑：用 `(Console|PC).{0,20}区间` 正则扫「是否按平台拆区间」会**误命中口径说明句**「Console 与 PC 价格盒渲染出完全相同的区间」。同类合规正则必须打印命中原文，不可只看布尔值。
- 字段校正：研究 JSON 顶层汇总键是 **`counts`**（不是 `summary`），逐卡为 `fc26`/`fc26Launch`/`fc27Current{console,pc,valid,representative}`/`fc27Min`/`fc27Max`/`condA`(null=不适用)/`condB`/`advice`/`maxRatio`。旧记忆里的 `summary`/`fc26Open`/`fc27Range` 三个键名均不存在。

## 15:26–15:40 球员头像全站接入（本轮）+ 两处错配修正 + 重新合并发布

- **用户需求**：主页所有球员相关监控（市场概览 / 市场扫描 / 进化专栏 / 传奇英雄监控）都要显示球员头像。
- **链路**：`shared/lib/player-avatar.mjs`（7 源索引，匹配序 cardId→slug→姓名→中文名→姓词元）→ 渲染器输出 `<img class="pimg" loading="lazy">` + 页头「球员头像 X/Y」→ `materializeAvatars` 用 `sips -z 48 48` 落盘 `reports/daily/D/assets/players/` → `report-assets.mjs#inlineLocalReportImages` 两遍内联（第二遍覆盖 market-scan 的 `db-data` JSON）→ `coordinate.mjs` 合并成单文件站点。**头像键 = EA resourceId，不是 URL 的 cardId。**
- **补全**：`backfill-avatar-keys.mjs D` 走 FUTBIN 同源 `fetch('/27/playerhover/<cardId>')` 补 `cardId→头像键`（单并发 + 450ms，批 15 条 < CDP eval 30s 上限；长会话连续 3 批失败即 `/close`+`/new` 重建会话）。本轮补回 **23 条映射 / 22 张图**，索引累计 **317 条**，图片库 2939 张。
- **发现并修正两类「配错人」（本轮最重要的教训）**：
  1. **同姓串图**：只按「姓」词元兜底会把同姓另一人配上去——实测 `Oh Hoo Sung` 与 `Cho Gue Sung` 解析到同一张图、`Ethan Mbappé` 命中 Kylian、`Micah Richards` 命中 Chris Richards、`Ledley King` 命中 Joshua King。修法：`resolve()` 的兜底匹配加**姓名词元双向包含守卫**（条目词元 ⊆ 该键登记姓名，或反向；两向都不成立即放弃）。收紧后覆盖率短暂下滑，**用 playerhover 权威补回，不放宽守卫**。
  2. **跨代串图**：FC26 参考对比区（93 张）用 FC27 头像库配图，82 张里至少 **6 张配错**（Richards/King/Scott/Doumbia/De Rossi）。修法：`render-icons-heroes.mjs` 对 `__game==='fc26'` 显式返回空图并在页面写明原因；`backfill-avatar-keys.mjs` 聚合英雄数据时排除 `heroes/data/**/fc26/**`；清理 `avatar-index.json` 中 **59 条仅由 FC26 英雄卡产生的跨代映射**（353→294 条）。
- **最终覆盖率**：market.html **50/50** · market-scan.html **738/750** · evolution.html **487/500** · icons-heroes.html 传奇 **131/131** + FC27 英雄 **48/50**（FC26 参考区按口径不配图）。剩余缺口均为 `playerhover` 返回 `noface` 的真实缺口。
- **快照与发布**：market/evolution/icons-heroes 三模块各重走 `prepare-rerun → begin --rerun → 本轮真实重开 FUTBIN 来源页（记录新 openedAt）→ 重渲染 → finish partial`；`coordinate --prepare-rerun/--rerun` → merge=success、failedPanels=0，index **22,441,726 B**（较上版 +10.3 MB，1941 张头像全部内联 data URL，无断链）→ sites 发布 → `/index.html` 与本地逐字节一致。**根路径仍走旧边缘缓存（12,118,761 B / sha 83f8c016），属既知 3–9 分钟收敛现象**。
- **排查手法备忘**：新建 `/tmp/verify-all-faces.mjs` 做「渲染出的 (头像键, 显示名) 对 vs canonical 姓名」一致性扫描——但 canonical 自身有**重复 resourceId 缺陷**（190042 同时登记马拉多纳与 Sergio Agüero；168473 同时登记贝肯鲍尔与 Rummenigge），会把正确结果误报为疑似，核对时必须遍历该键的**全部**登记名，不能只取最后一条。APOSTROPHE 类名字（`Eto'o`）比较前要过 HTML 实体还原 + 去重音 + 转小写。
- **契约同步**：根 `AGENTS.md`「球员头像强制规则」补「姓氏词元双向包含守卫」「跨代（FC26）数据一律不配头像」两节与最新覆盖率；`apps/market/AGENTS.md` 补「头像两条红线」。

## 16:23–16:35 FC·传奇价格区间 T16（每小时子任务，未发布站点）

- 四步全绿：自检 exit 0 → 采集 → `build-icon-research.mjs` → `record-icons-daily.mjs` + `render-icons-heroes.mjs`。
- **131/131 成功**（含一次重跑），`errors 0`、`missing 0`；区间 **69,000（瓦拉内）– 15,000,000（贝利）**，`scope=card`、`priceBasis=listing-estimate`；详情页有效当前价 **26 张**（Console 22 / PC 6），较 T15 的 20 张继续上升。
- **区间首次跨小时滚动**：仅 1/131 —— 尤西比奥 `da-silva-ferreira` min 71,000→72,000、max 15,000,000→13,100,000。
- 研究：投资建议 **2 张**（条件A 加林查 `lima-do-amor` 289,071 > 129,077；条件B 斯科尔斯 231,598 > 区间上沿 200,000），无 FC26 对照 14 张，条件A 仅 5 张可判定。
- **传输层劣化故障第 2 次复现**（首轮 104/131 卡死、补采 0 张；`Runtime.evaluate` 超时 ×8 + JSON 截断 ×32）：恢复套路 = `check-deps` 确认 → `--limit 8` 试跑（写 `dryrun/`，有防呆）→ 重跑全量，一次成功。**`check-deps.mjs` 测不出这种劣化，不要依赖它报警。**
- **结构坑**：`daily/<D>.json` 逐卡数组键是 `players`，hourly/latest 才是 `cards`；用错会误判「快照 0 张、区间未合并」。
- 产物：`reports/daily/2026-09-17/market-icons-research.html`（45KB，含两条免责 + 全部口径要素）、`icons-heroes.html`（231KB，三列 `当前价(列表页)|最低价|最高价`，无按平台拆区间列，表头无「区间」平台列）、`apps/market/engine/icons/data/research/fc26-vs-fc27-2026-09-17.json`。未调用 run-state（每小时任务禁用其每日排他锁）。


## 市场监控改造：每小时价格 + 关注列表（16:17–16:40）
- 用户新增需求后落地：新增「FC·市场价格关注列表（每小时）」自动化（id c32edfd3-a4a7-4ea1-9f25-40f7a91d21ab，FREQ=HOURLY;INTERVAL=1），03:00 每日全量任务保留。
- 新链路：collect-market-prices.mjs（/27/popular 双平台价+热度、/27/popular/evolutions 热度 → data/prices/fc27/{popular,evolutions}/{hourly,daily}/，含 last-attempt.json 与 45s 退避重试）→ build-market-watchlist.mjs（watchlist.json + players.json，评分=0.45热度分位+0.40同档相对价+0.15变动分）→ apply-market-name-zh（--file 支持多文件；新增 lists.* 段注入）→ render-market-watch.mjs（market-watch.html）+ render-market-report.mjs（扫描页价格刷新）。
- 站点：市场栏目第三子标签「关注列表」；接线 = merge_daily_report.mjs + coordinate.mjs extras（['market-scan.html','market-watch.html']）+ dashboard.mjs 描述；run-state 不动（market-watch 与 market-scan 同为「当日附属产物」live 复制）。
- 口径决策：变动分只取两个真实整点观测（basis=hourly）；与 03:00 开盘基线的单点对比只展示不计分（moveSuspicious 标 |Δ|≥50%）；不计算日环比/累计涨跌。players.json 保持日报名单构成，只追加新出现的有价卡。
- 已实测：16 时采集 250 卡（C 188/P 192 有效价）+ 500 进化卡；关注页 140 条、127 含中文名；扫描页 786 人；36 项回归测试全绿；coordinate --rerun 合并成功。**未发布**（站点明日 03:05 汇总发布自动上线；coordinate 已两次 --rerun，publish=delegated）。
- 踩坑：① 构建器重建 watchlist.json 会抹掉已注入的 nameZh——顺序必须 build → inject → render；② baseline 需在逐小时序列处理前加载（ReferenceError 踩过一次）；③ /27/popular 的 .item-score-segment 是卡片级 Item Score（两平台同值），非成交价。

## 新增栏目「FC26 球员回顾」（傍晚，用户指令：主页加一栏 / 不联网 / 读本地 FC26 数据 / 按能力值汇总价格变化图 / 可复用旧页面）
- 新脚本 `apps/market/engine/scripts/render-fc26-review.mjs`：只读本地 FC26 三池（gold `fc26-first-month.json` 152 张 / icon `base-icons.json` / hero `base-heroes.json`），按 OVR 六档（95+ / 90–94 / 88–89 / 86–87 / 84–85 / ≤83）聚合，产出 `apps/market/engine/gold/reports/fc26-season-review.html`（169 KB，4 张**手写内联 SVG**：价格指数走势 / 涨跌幅双向条 / OVR×卡池热力矩阵 / 峰谷时点；不用 CDN）。窗口 2025-09-18~10-17（30 天），样本 351 张。**结论：能力值越低越抗跌**（≤83 中位 −11.8%、上涨占比 35%；95+ −60.4%、上涨占比 0%）；**高评分卡更早见顶更晚止跌**（95+/90–94/88–89 峰值中位 D1、谷值 D27–28；≤83 谷值 D8）；180/351 卡峰值落在开服首日。
- 能力值口径：**优先 `database/fc26.json` 的 FC26 rating**（141 张），库内无记录才回退池内 OVR（210 张，gold 用 `fc27Rating`），页面按 `ratingSource` 注明。**本栏目一律不配头像**——FC26 的 `resourceId` 跨代不可信（实测 5/5 同 id 姓名与 canonical 全不符，如 rid 51 库里写 Dembélé 实为 Shearer），遵守跨代红线。
- 三处接线：`apps/portal/dashboard.mjs`（`ICONS.review` / `FC26_TAB`+`CARD_ITEMS` / `navItems` / `fc26View`+模板插入 / 首页右栏 `.review-rail` / hero 文案「六个板块」）、`apps/portal/merge_daily_report.mjs`（`FC26_SOURCE` + `buildFc26ReviewPanel()`，**不参与当日日期校验**，存在即 `panelStates='ok'`）、`automation/coordinate.mjs`（隔离目录准备阶段新增 `cpSync apps/market/engine/gold/reports/` —— **漏了这步隔离合并会读不到底稿**）。根 `AGENTS.md` 已同步（导航七个入口改八个 + 新增该栏目条目）。
- 核验：36 项测试全绿（execution 6 / news-media 14 / regression 13 / verify-publication 3）；`coordinate --rerun` 合并成功；CDP 实机确认导航 8 项含「FC26 球员回顾」、首页卡片徽标「当日已收录」、右栏 review 卡、`view-fc26` 可切换、iframe 内 4 SVG / 418 行 / 5 个 card 区、无 NaN。**本轮未发布**（用户未要求）。
- 踩坑：① coordinate 隔离目录（stage）**只复制白名单目录**，新增常驻底稿必须同步加 `cpSync`，否则本地跑得通、隔离合并读不到；② `copyFileSync` 归档再次 EIO（`daily-merged/archive/2026-09-15.html`），重跑 `--rerun` 一次即恢复（已知问题，第二次踩中）；③ Bash `grep` 在本机**恒返回空**（连纯 ASCII 模式也失效），一律改用 Grep 工具。

## 进化专栏（evolution）改版 2026-09-17 16:30–16:40 · partial
- 用户要求：展示进化卡+当前热度/价格；不要全量、按价格排序；按位置/价格/进化区分，每条进化只出热门 Top5。
- **来源侧硬事实（四路交叉核验，勿再重复排查）**：FUTBIN 对进化卡**不公布挂牌价**。① `/27/popular/evolutions` 卡片内 `.popular-price-wrapper`/`.platform-ps-only`/`.price-segment` 命中数全 0；② 对照组 `/27/popular` 有价（250 卡 → 250/250/500 命中，卡面以 `0|4.1K|...` 开头）→ 证明是来源侧设计而非通道问题；③ `/27/players/evolutions` 无价格列（COST 是进化解锁费用）；④ playerhover 与进化版详情页均无 Console/PC 价。
- 字段辨析（此前存错）：`futbinListValue` 实际是 `div.playercard-27-futbin-rating`（**FUTBIN Rating**，非价格）；已新增正确字段 `futbinRating`，旧名仅兼容保留。火苗旁数字才是热度（`popularityCount`）。
- 价格口径定案：按球员 ID 关接市场模块当日**基础卡**双平台价作「持仓成本参考价」（PC 价优先、PC 为 0 取 Console），只在页面标为参考价；关接不到如实显示「无报价」。当日关接 45/500，精选池 70 人中 14 人有报价。
- 交付：新增 `apps/market/engine/scripts/enrich-evolution-prices.mjs`；重写 `render-evolution.mjs` 为「精选池 × 三视角」（按进化 Top5 / 按位置 / 按价格档＋本日速览 KPI）；选取只按热度（同热度用 Rating+姓名兜底，**不让价格影响最热 5 人**）。产物 `reports/daily/2026-09-17/evolution.html` 由 333 KB → 约 124 KB，行数 500 → 182，头像 69/70。契约 `automation/prompts/evolution.md` 新增「价格口径」「版式要求」两节。36/36 测试通过。快照 SHA-256 `eaeff90a…`（runId d3cf1981）。
- 踩坑复现：`begin --rerun` 会把 `automation/runs/D/<module>/` 下内容（含 evolution.json 本体、work/、evidence.json、report.html）整体 rename 进 attempts/，重跑前必须先拷回；`evidence.sources[].openedAt` 必须 ≥ 本轮 startedAt，上一轮打开的页面时间全部无效，要**在本轮内重新打开**来源页。
- 追加「逐卡点开展示一个月价格曲线」（用户傍晚追加指令）：`render-fc26-review.mjs` 里代表球员表 + 全量明细的**每一行**都可点开，展开该卡开服首月 30 天逐日曲线（Cross 实线 / PC 虚线 / 峰·谷·末标记 / 统计行），全量明细带球员名筛选框（405 行 → 命中 3 行验证通过）。**惰性渲染**：渲染期只输出 `data-curve`（30 个价格点的紧凑 JSON，约 0.59 KB/行），点开时才由页内脚本画 SVG —— 底稿 **539 KB**（预渲染版实测 1863 KB，省 3.5 倍）；`index.html` 19.3 MB（目标 <50 MB）。CDP 实机在内嵌 srcdoc 的站点页里验证：点开 `table-row`、30 点路径、PC 虚线、标注与统计行全部正确，筛选可用。
- **踩坑（重要）**：`<script>` / `<style>` 是 raw text 元素。`themedPanel` 的 `&`→`&amp;`、`"`→`&quot;` 对 srcdoc 属性只是一次往返编解码（属性值会被 HTML 解析器解码），理论上双引号也能还原；但为了不让脚本依赖这条链路，**脚本内一律用单引号**，引号字符用 `String.fromCharCode(39)` 构造，生成的 SVG 属性也全用单引号（`x1='78'`）。另：`class="pl-cap"` 这类双引号是唯一残留，已统一。

## 17:31 market-hourly 第 3 次运行（17 点整点）
- check-deps exit 0；collect-market-prices 一次成功：热门榜 250 张（Console 有效价 213 / PC 218 / 双有效 210）、进化榜 500 张全有热度，无错误。
- build-market-watchlist：hourlyPoints=2（16→17 点）；watch 30 / undervalued 20 / trending 20 / pendingPrice 50。
- 踩坑：apply-market-name-zh 的 --file 传 `runs/D/` 字面路径读不到，须传 `runs/2026-09-17/`（已记入 MEMORY.md）。
- 词库追加 10 条译名（Calafiori/Maatsen/Vivian/Zoe Matthews/Antman/Browne/Damiani/Sych/Thomas-Asante/Aranda），重跑后未命中 0；核对 Rank3 Shaw=khadija-shaw URL 配对正确（卡蒂嘉·肖，90 总评）。
- 渲染 market-watch.html + market-scan.html 成功（头像新落盘 26 张）。未发布站点、未动 market.html。
- 16→17 点价格变动 128 张（升 48 / 降 80）；最大跳变 Thuram +185.7%（4200→12000）、Messi +129.1%（29250→67000）、Martinelli −93%（40000→2800）。

## 17:35 icons-pricerange-hourly T17（传奇卡价格区间）
- check-deps exit 0；采集终值 **131/131**，区间 69,000（VARANE）– 15,000,000（PELÉ `arantes-nascimento`），口径 listing-estimate、scope card；较 T16 区间变动 0 张。有效当前价 26 张（Console 23 / PC 7）。
- 研究：投资建议 2 张（条件A 加林查 289,071>129,077；条件B 斯科尔斯 231,598>上沿 200,000；双命中 0），无 FC26 对照 14 张；条件A 可判定 5 张、条件B 117 张。
- **重大根因定位（本次最有价值产出）**：`collect-icon-priceranges.mjs` 的传输层劣化（`Runtime.evaluate` 超时 + `Unexpected end of JSON input`，补采 0 张）此前被记为「偶发」，本次第 3 次复现后确认为**长驻宿主页/单目标级资源累积**：脚本每轮只建 1 个宿主页供全部 17 批复用，劣化总在「某进度之后」出现，而单批 `--limit 8` 永远成功（新页容量足够）。劣化点 16 / 104 / 48 三次实测。
  - **修复**：新增 `HOST_RECYCLE_EVERY = 3`，每 3 批重建宿主页（`extract-icon-priceranges.js` 是无状态 `fetch`+`DOMParser`，只依赖 futbin origin，重建安全）。同健康度下产出从 48 提升到 107，补采补齐 131/131。未动串行/限速/并发语义。36 个回归测试全通过。
  - 旧「三步恢复套路」（check-deps → limit 8 探测 → 重跑全量）**本次不再充分**（重跑仍只到 48），已由上述脚本级修复取代。
- 顺带修复：`errors[]` 原样落盘会把**补采已成功**卡的首轮错误一并写入（T17 残留 24 条），使完整快照被误读为部分失败；改为过滤已成功项。该字段不流入 daily 快照与页面（已核验），故未为此重跑。
- 产物：`market-icons-research.html` 45KB、`icons-heroes.html` 231KB（表头 `当前价(列表页)|最低价|最高价`，按平台拆的区间列 0）。未发布站点、未动 publish-status、未调用 run-state.mjs。

## 合并发布（17:37–17:59）· 成功
- 用户要求「跑合并并发布到 FC27每日情报台」。`node automation/coordinate.mjs 2026-09-17 --rerun`（当日锁已存在，必须带 --rerun）；merge=success、failedPanels=0，indexSha256 `68d64c9d…`（20,366,817 B）。进化专栏新版已随本次合并进站点（16:45 那次 coordinate 已带上，但当时 publish=delegated 未实际发布）。
- 发布走 `workbuddy_sites_deploy`：`directory=/Users/wuyanzu/Desktop/FC/daily-merged`、`entryHtml=index.html`、`language=static`、`updateExistingApp=true`、`userAskedToPublish=true`、`domainPrefix=fc27-daily-intel`。分享链接不变。
- 验证：`node automation/verify-publication.mjs 2026-09-17` 连续 3 次 verified=true（matchesLocal/hasArchiveLink/hasDateAnchor/completeHtml 全 true）；线上 index 含「按进化精选/按位置精选/按价格档精选/本日速览/参考价 C/P/六维合计」，无旧版「一、热门进化卡（500 张）」残留，无本机路径；archive/2026-09-17.html 200 且同新版；assets/yanzu-banner.jpg 200。
- **缓存坑（本次最耗时）**：该 host 存在多个边缘缓存条目并存，同一 URL 会漂移。用响应头区分：新版 `last-modified=09:38:23Z`、`age` 小；旧版 `last-modified=07:38:32Z`、`age` 递增到 ~1215s。实测 10 次请求命中新版比例 8/10 → 5/10 → 4/10 → 最终收敛。**判据不要只看一次 curl**，也不要据此判发布失败；re-deploy 只能加速不能立即清掉旧条目，按契约最多重试两次即可。
- **注意 `verify-publication.mjs` 会重写 `automation/publish-status-D.json` 并把 reason/publishResult 重置**，所以要在最后一次 verify 之后再写业务字段，否则被覆盖。权威记录已更新为 publishResult=workbuddy-sites-deploy。

- 18:36 market-hourly 18 点整点运行成功：热门榜 250 张（Console 有效价 208 / PC 214）、进化榜 500 张全有热度；watch 263 / undervalued 85 / trending 211 / pendingPrice 986，hourlyPoints 16→18。译名补 13 条（含 Lucca 洛伦佐·卢卡、Bellegarde 贝勒加德等）至 622 条后未命中收敛 0。17→18 点显著跳变：Martinelli 双平台 +500%~+793%（2500→15000 / 2800→25000）、Cherki PC +107.8%；Karchaoui PC −52.6%。watch 列表内 moveSuspicious=0。

- 18:52 icons-pricerange-hourly T18 运行完成：**131/131 成功**（首轮 91 + 补采 40），`errors 0`、`missing 0`。区间 69,000（VARANE）– 15,000,000（PELÉ `arantes-nascimento`），口径 listing-estimate / scope card。**T17→T18 区间变动 0 张**；有效当前价任一 26 张（Console 24 / PC 6），与 T17 持平。研究：投资建议 2 张（条件A 1 = lima do amor 289,071>129,077；条件B 1 = scholes 231,598>200,000；双命中 0），无 FC26 对照 14 张。产物：research 45.3KB（免责+重演+不适用+listing-estimate 齐备）、icons-heroes 231.2KB（表头无按平台拆区间列，日环比/累计涨跌 262 格全为「—」，无伪百分比）。未发布站点、未调用 run-state。
- **传输层劣化第 4 次出现，`HOST_RECYCLE_EVERY=3` 后仍会发生**：本轮劣化点在进度 32（32→48→56 停滞）与 80/104/112 多处，最终首轮仅 91 张，补采 40 张全部一次成功。说明「每 3 批重建宿主页」只是延后而非消除劣化；**判据仍为「某进度后连续批全失败 + 补采一次成功」→ 直接依赖内置补采轮，不要调并发、不要反复补采**。

## 19:40 market-hourly 第 19 点整点
- 采集成功无 403：热门榜 250（Console 210 / PC 214 有效），进化榜 500。
- 词库 +16：Bakoune=亚当·巴库内（百科核实）、Lola Brown=洛拉·布朗（纳米数据核实）、Igamane=伊加马内、Jesé=赫塞·罗德里格斯、Mbangula=姆班古拉、Setford=塞特福德等，未命中收敛 0。
- 确认：build-market-watchlist.mjs 只写 watchlist.json，players.json 属 03:00 市场任务产物，hourly 侧「跳过」为预期行为。
- 18→19 时段显著变动：德布劳内 PC 4700→2800（-40.4%）、Console 2100→1600（-23.8%）。

## 19:50–20:0x 收尾：6 个 WorkBuddy 定时任务的统一行情口径同步（承接 Codex 未完成部分）
- 背景：Codex 在**已改完本地契约**后中断——只完成 2 个每小时任务（市场关注列表 / 传奇价格区间）的自动化提示词改写，尚有 4 个每日任务（传奇英雄监控、市场监控、进化专栏、汇总发布）保留旧长提示词，且已改的 2 个是英文、与仓库中文契约不同源。
- 本轮动作（只改 WorkBuddy 定时任务提示词，不动代码）：6 个市场相关任务全部改写为统一骨架——「先读根 AGENTS.md 对应强制规则 + 统一当前行情与禁止重复分析小节 → 再读 automation/prompts/<X>.md → 本地文件是唯一契约、与旧任务记忆冲突以本地为准」，正文对齐 `automation/task-definitions.json` 的 `bootstrapPrompt`。任务总数仍为 9，**未新增任何重复任务**。
- 关键去重条款已逐任务落地：市场/进化「禁止生成 players.json / priceRef」；进化「只写 baseCardId，价格只在页面运行时按 baseCardId 读 current.json」；传奇监控「逐日快照只作台账，实时价与区间由每小时任务写 current.json，不得再开详情页重复采集」；汇总发布「current.json 必须随站点部署，禁止二次价格分析」。
- 复核结果（本轮独立验证）：① 测试 `node --test automation/{execution,regression,verify-publication,news-media}.test.mjs` **38/38 通过**（Codex 上次记录为 37）。② 传奇快照 vs 统一行情：131 张 × 2 平台价 = 262 个单元格**差异 0**，131 个卡级区间**差异 0**（`record-icons-daily.mjs` 经 `latestPlatformPricesOf()` 从逐小时详情页快照传播，已实测生效）——原审计问题 2「传奇实时价没有传播」确认修复。③ current.json 现有 **948 张卡**（Console 有效价 272 / PC 264），较 Codex 交付时的 930/260/252 随整点任务自然增长。④ 5 份页面产物（market / market-scan / market-watch / evolution / icons-heroes）均含 `assets/data/current.json` 运行时读取片段，reports/daily/D/assets/data/current.json 与 daily-merged/assets/data/current.json 均已落盘。
- 遗留（本轮未动，需用户决策或单独立项）：① canonical `shared/data/fc27/players.json` 仍有 **11 组不同姓名共用 resourceId**（Mainoo/Tobin Heath、Mitoma/Nagasato、Tomiyasu/Ellen White、Suárez/Maciel Mota、Tanaka/Sinclair、Maradona/Agüero、Zidane/Varane、Beckenbauer/Rummenigge、Angerer/Nadine Angerer、da Silva Ferreira/Kepler Laveran 等）；实测这些冲突中**只有首位 owner 的 cardId 出现在 current.json**（21487/21491/21503/21507/21548），cardId 优先解析下当前渲染配图正确，属**潜伏缺陷**（第二 ID 段卡一旦入榜会配错头像），修法应在 canonical 构建侧而非删行。② 英雄库 50 卡仅 25 个唯一 slug、缺版本字段，渲染识别仍可能歧义（价格侧按 cardId 已无影响）。③ **线上站点仍为每日 03:05 发布**：逐小时只更新本地 current.json，线上页面刷新拿到的是最近一次发布版本；要实现「刷新即最新」需把「重发布 daily-merged」并入小时任务（每天 24 次发布）——已作为待决策项提交用户。④ 无 SkillManage 工具可用，故未沉淀新 skill；既有 fc-* 技能未发现需要修正之处。

## 用户决策：线上行情改为「每小时随采集重发布」（本轮已落地契约）
- 用户在两选项间选择**每小时重发布**（而非保持每日发布）。落地点：`market-hourly` 第 6 步——采集/打分/渲染后重发布 `daily-merged/`（更新同一应用 FC27每日情报台、`updateExistingApp` + `userAskedToPublish` + `domainPrefix=fc27-daily-intel`、分享链接不变），只上传「已存在的合并目录 + 最新 assets/data/current.json」，**不重跑 coordinate.mjs**（避免重写 index.html/archive）；结果记 `automation/runs/D/market/publish-hourly.json`；**不跑 verify-publication.mjs**（它会重写 publish-status-D.json 这一每日权威记录）；`icons-pricerange-hourly` 自身不发布，其传奇价最迟下一个整点随该次重发布上线。
- 契约三处＋定时任务已同步：`AGENTS.md`（含新增「防重复站点红线」）、`prompts/market-hourly.md`（第 6 步 + 产物表 + 预算 10→14 分钟 + 回复要求）、`prompts/icons-pricerange-hourly.md`、`prompts/daily.md`、`prompts/publish.md`、`prompts/README.md`、`automation/task-definitions.json`（market-hourly bootstrapPrompt）、WorkBuddy 定时任务 c32edfd3。测试仍 38/38。
- **本轮实查线上现状（重要）**：`https://fc27-site.app.workbuddy.host/assets/data/current.json` 返回 **HTTP 404**（335 B 错误页），线上 `index.html` HTTP 200 / 20,366,817 B（2026-09 版式）。说明线上仍是 17:37 那次发布的老版本，**不含统一行情资源**，线上页面刷新目前拿不到 current.json。首个整点重发布（c32edfd3，下一跳约 20:47）将同时推送新版式与 current.json；若那轮发布失败，需在 FC 工作区的会话里手动重发一次。
- 本轮试跑重发布失败原因（已规避）：在本会话（工作区为 `/Users/wuyanzu/WorkBuddy/2026-09-17-19-50-30`，非 FC 目录）调用站点发布返回「this workspace has no existing app to update」——**发布应用记录按工作区绑定**，只能在 FC 工作区/该任务的 cwds 内更新既有应用；据此在契约与任务提示词中都加了「无既有应用即停止、禁止新建应用/换域名」的红线。
- 20:47 market-hourly（T20）：采集成功，热门榜 250 张 Console 204/PC 208 有效价，进化榜 500 张全有热度；watch 346/undervalued 77/trending 237，整点观测 16–20 五点；Top3 帕约尔 90、哈兰德 89、基米希 82；译名补 13 条收敛至 1（Kim Myung Soon 汉字无法核实保留英文）；渲染 watch/scan 两页；发布 fc27-site 成功，线上 current.json generatedAt 与本地一致（12:47:54Z）。

- 20:5x icons-pricerange-hourly（T20）：自检 exit 0，4 步按序完成，**131/131 首轮即成功**（17 批 0 重试、errors 0、missing 0），无需补采轮——5 次宿主页重建（每 3 批）全程零 CDP 劣化。区间 69,000（VARANE）– 15,000,000（PELÉ）；listing-estimate / scope card。T18→T20 区间变动 **0 张**；任一平台有效当前价 26 → **32**（Console 31 / PC 4）。
- 投资建议 2 → **3 张**：`charlton`(3,323,810>2,499,000 condA)、`bergkamp`(505,048>300,000 condA)、`scholes`(231,598>当前 200,000 = condA；>区间上沿 200,000 = condB，**首个双命中**)。`lima-do-amor`（加林查）掉出——详情页当前价 129,077→1,289,000 已高于 FC26 开服价 289,071。属实时价滚动导致的正常结论更替，非缺陷。condA 可判定 5 → **31**（=32 有效价 − 1 张无 FC26 对照），**「条件A 只有 5 张」应改述为随当日有效价浮动**。
- **旧「已知待决」项结案**：`record-icons-daily.mjs` 现已取详情页当前价（快照有效价 Console 31/PC 4，与 current.json **32/32 逐卡一致**；旧口径 03:00 列表页仅 6 张），监控页当前价不再落后于研究口径。AGENTS.md 中相关描述已过期。
- **T19 快照缺失**：hourly 目录只有 T11–T18 + T20，无 `2026-09-17T19.json`，19:20 那轮未产出（非本任务可控，如实记录）。
- 校验：三处区间一致性 current.json↔daily 不一致 0 张；research 七要素齐备、无按平台拆区间；监控页 0 个伪百分比 + 0 个按平台区间列（唯一正则命中为口径说明句假阳性，同 T15）；头像 131/131。仅改 `icons-heroes.html`/`market-icons-research.html`（20:55）；`publish-status` 与 market.html 未动；未调 run-state.mjs、未发布。
- 文档漂移（非缺陷）：`build-icon-research.mjs` 写 `icons/reports/fc27-icon-live-research.html`，AGENTS.md 只提 `fc27-icon-analysis.html`；`merge_daily_report.mjs` 108–109 行回退顺序为 live-research 优先，接线正确。

## market-hourly 21 点整点（21:51–21:58）— 成功（含发布）
- 自检 exit 0；采集一次成功：热门榜 250（Console 有效价 219 / PC 209），进化榜 500 全有热度，无 403。
- watchlist：watch 334 / undervalued 77 / trending 244 / hotEvo 531 / pendingPrice 959；整点观测 16–21 六点。
- Top3：长谷川唯 92、芭芭拉·班达 91、布卡约·萨卡 87。
- 译名补 14 条（Aboukhlal、Capaldo、Edozie、Gelhardt、Igor Jesus、Kitolano、Lozano、Manu Fuster、Methalie、Mundle、Rodrigo Muniz、Roka、Shahar、Varela Djamanca）后未命中 0。
- 渲染 watch/scan 成功（头像本轮新落盘 7 张）；第 ⑥ 步发布成功，链接不变，线上 current.json 200 且 generatedAt=13:51:23Z 与本地一致（文件 814375B，cards 为 cardId 映射非数组），已记 publish-hourly.json T21。

## icons-pricerange-hourly 21:57（落盘 T22）— 成功
- 自检 exit 0；4 步按序完成，**131/131 首轮即成功**（4m10s、17 批 0 重试、`counts.missing 0`、`errors 0`、逐卡 `ok=false` 0）；5 次宿主页重建全程零 CDP 劣化，未触发补采轮。区间 69,000（VARANE）– 15,000,000（PELÉ）；listing-estimate / scope card。
- T20→T22 区间变动 **0 张**；任一平台有效当前价 T20 32 → **T22 37**（Console 35 / PC 5），平台价继续滚动放出。
- 投资建议 **3 张**（与 T20 同集）：`charlton` FC26 3,323,810 > 当前 2,300,000（condA）、`bergkamp` 505,048 > 300,000（condA）、`scholes` 231,598 > 当前 200,000（condA）且 > 区间上沿 200,000（condB，双命中 1）。无 FC26 对照 14 张、无区间 0 张。
- 校验：`current.json` 中传奇 131/131 命中，逐卡 `priceRange{scope:card}` + `platforms.console/pc`（source `futbin-icon-detail`），与 hourly T22 **区间 0 不一致、当前价不合 0**；daily 快照 `players` 131/131 含区间、双平台键齐备，与 current 区间不一致 0；research 50.7KB / icons-heroes 313.4KB（表头 `当前价(列表页)|最低价|最高价`，区间列唯一且 title 标「卡级：Console 与 PC 渲染同值」，0 个伪百分比、1388 处「—」，头像 131/131）。
- **T19 与 T21 快照均缺失**（hourly 现有 T11–T18 + T20 + T22），19:20 与 21:20 两轮未产出；本轮于 21:57 启动故落 T22。非本任务可控，如实记录。
- 仅覆盖当日 `daily/2026-09-17.json`（09-16 未变）；`publish-status-2026-09-17.json` 仍为 18:01；未发布站点、未调用 run-state.mjs、未渲染 market.html。

## 22:57 market-hourly（22 点整点）
- 采集成功：热门榜 250 张（Console 211 / PC 194 有效价）、进化榜 500 张热度全有；无 403。
- watchlist：watch 30 / undervalued 20 / trending 20 / pendingPrice 50；Top3 班达 85 / 戈登 85 / 哈兰德 82。
- 译名补 10 条（含 Cryzan→克雷桑）后收敛 0；渲染 watch/scan 成功。
- 第 ⑥ 步发布成功：链接不变，线上 current.json 200 且 generatedAt=14:54:39Z 与本地一致，publish-hourly.json 已记 T22。

## 23:0x icons-pricerange-hourly（T23）
- 自检 exit 0；4 步按序完成。**131/131 首轮即成功**（4m02s、17 批、5 次宿主页重建，零 CDP 劣化、未触发补采轮），`counts.missing 0`、`errors 0`。区间下沿 69,000（VARANE）/ 上沿 15,000,000（PELÉ `arantes-nascimento`）；口径 listing-estimate、scope card。
- **T22→T23 区间变动 0 张**；任一平台有效当前价 T22 37（Console 35/PC 5）→ **T23 42（Console 40 / PC 10）**。
- 研究：投资建议 **4 张**（condA 4 / condB 1 / 双命中 1）：scholes 231,598 > 200,000（双命中）、kroos 1,128,656 > 850,000、charlton 3,323,810 > 2,100,000、bergkamp 505,048 > 500,000。**kroos 为本轮新增**，因其 Console 当前价由 T22 的 2,100,000 跌至 850,000（真实价变动驱动的结论更替，非缺陷）；无 FC26 对照 14 张、无区间 0 张。
- 校验：current.json 传奇 131/131 命中、`priceRange.scope=card`+`source=futbin-icon-detail` 全合规、双平台键 131/131；current ↔ T23 快照区间不一致 0、current ↔ daily 区间 0、双平台当前价 0；daily `players` 131/131 含区间与 console/pc，顶层 `priceRange.scope=card`；icons-heroes 305KB 表头 `当前价(列表页)|最低价|最高价|日环比|累计涨跌`、伪百分比 0、1378 处「—」、头像 131/131；research 49KB 七要素齐备。
- 未发布站点；`publish-status-2026-09-17.json` 仍 18:01；未调用 run-state.mjs；未渲染 market.html；current.json 内市场侧条目（futbin-popular 250 / daily-market 750）未被破坏。

## market-hourly 23 点轮（23:57，成功含发布）
- 自检 exit 0；采集一次成功无 403。热门榜 250 张：Console 有效价 202、PC 190；进化榜 500 张全有热度。
- watchlist：watch 318 / undervalued 85 / trending 231 / pendingPrice 972；hourlyPoints 16–23 共 8 点。Top3：戈登 84 / C 罗 83 / 哈兰德 81。
- 译名补 9 条后未命中收敛 0（Banerjee、Macalou、Mejía、Baleba、Gabri Martínez、Gomel、Hanson、Kiko Bondoso、Krattenmacher）。
- 渲染 watch/scan 两页；第 ⑥ 步发布成功（链接不变），线上 current.json 200 且 generatedAt 与本地一致（15:57:37Z），publish-hourly.json 已记 T23。未跑 run-state、未渲染 market.html。

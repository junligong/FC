- 资讯采集（03:00 轮）：16/16 打开、入库 48 条、译文 48/48、图片落盘 49/49、「待翻译」0，产物 reports/daily/2026-09-21/news.html。终态 partial：把「easportsfc 0 条 / EASFCDirect 窗口外」两条如实注记误写入 evidence.missingItems 触发自动降级（应放 notes），且 finish 终态后不可重提覆盖。教训已记入 automation memory。

## FC·市场监控 03:13（runId 37a347ba，partial）
- 采集全成：popular 250（Console 198/PC 177 有效价）、evolutions 500、/27/players 120 行（validPS 105/PC 78）；priceBasis=partial-live。
- 译名 +158 条（含旗手怜央、蒯纪闻等），命中 1833/1833 未命中 0；扫描 574 行唯一 cardId，排除 10 张 Hall of FUT（新入池：Doumbia/Guarin）。
- 头像 +115 键/113 图，缺口 2 张为源站无图（Franco/Banda）；current.json 合并 750 张一次；watchlist + database-columns 均已渲染；83+ 补采未跑（预算收口）。
- 坑：`run-state.mjs begin market D` 的 D 字面值被拒，须传实际日期。

## FC·进化专栏 03:26（runId 97c127ed，partial）
- 采集全成（三页 HTTP 200，仅 8 秒）：榜单 500 卡 / 总览 **19 条路径**（09-20 为 18，新增 **Creative Crossroads**，付费 150 FC Points + 15,000 金币）/ expired 0 节点。priceEls=0 再次确证进化卡无挂牌价。
- **⚠ 总览页 DOM 又重构（本轮最大风险点，已修）**：`div.evolutions-overview-wrapper` 由扁平换行改为**深度缩进嵌套**，标签改**首字母大写**（Evolutions / Training Camp / Cosmetics / Rewards / Season 1 Level: N / Free / Repeatable），`Total Upgrades` 逐项由 4 token 合并为 2 token（`Overall` + `"+34 | 79"`），PATH token 变 `Path A → A → A`。旧解析会**四处同时静默失配**（requirements 全空 / upgrades 吞掉投票块虚高到 46 项 / 类型标签全空被默认成 Evolutions / FREE 读不到）。**修法 = 解析前空白归一化 + 一律大小写不敏感 + 升级块遇到首个纯百分比行截断**；另 `/^PATH/` 大小写敏感会吃掉一格使 (name,delta) 配对错位，表现为「37 个 token 却输出未列出升级项」。已同步 `web-access/references/site-patterns/futbin.com.md`。
- **踩坑（新）：`openedAt` 时区换算 bug**。collect-meta 里是 UTC ISO，写成 `new Date(z).toISOString().slice(0,19)+'+08:00'` 只换后缀不换算，会把 UTC 墙上时间当东八区 → 结果**早于本轮 startedAt**，触发 run-state「openedAt ≥ startedAt」校验失败。正解 `new Date(new Date(z).getTime()+8*3600e3)...`。修在 build-json 的 `toCst`。
- 译名：首轮未命中 33 → 32 条通行音译入词库（1858）+ 1 条经查证为 **Bao Shimeng = 鲍世蒙**（上海海港）→ 未命中 0（500/500）。
- 头像：backfill 两轮（+24 键 / +24 图），页头 **81/83**；唯一残留不可解析 = cardId 20509（源站 `noface`）——第二轮新增 0，属源侧事实，非可修缺口。
- 行情关接 **81/500、有有效报价 41**（09-20 为 110/53）。**降低属真实覆盖变化**：今日榜单由新路径 Creative Crossroads 主导（174 张，79 上限 CAM），其基础卡大量不在市场数据集内；已交叉核对 baseCardId 与 current.json 键（354 唯一 id 中 42 命中 → 81 行，21 个唯一 id 有价 → 41 行），口径自洽，**未用任何替代数据填充**。本任务未采价、未写 priceRef。
- 产物 `reports/daily/2026-09-21/evolution.html`（175,654 B，徽标 PARTIAL），快照 SHA-256 `b472e65f…`。校验：去重 500 唯一（url+进化名，0 重复）、nameZh 500/500、无 undefined/NaN、`priceRef` 仅出现在说明句、无价格副本字段。

## FC·传奇英雄监控 03:31（runId fd734deb，partial）
- 通道 triage OK 未自愈。`/27/players` **首次读通 1–15 页（450 行）**；16–26 页 403，退避 75s/150s 各重试 1 次（上限 2）仍 403 → 停手。Icon **131/131 全采到**（iconMissingFromSource=0），英雄卡首次由 50 → **99**（页 14/15 的英雄以往被 403 挡在外面）。
- **修掉一个静默错**：`render-icons-heroes.mjs#collectHeroes()` 递归扫 `heroes/data/**`，把 09-20 新增的**派生台账** `players/fc27/ledger-heroes.json`(50) 与采集产物 `prices/fc27/base-heroes.json`(99) 一起收录 → 英雄表 149 行、50 个 cardId 重复。修法 = 跳过 `ledger-*.json` + 按 cardId 去重兜底 → 99 张。**教训：凡按目录递归聚合的渲染器，都要排除派生台账。**
- 英雄卡「一名球员两张不同 cardId」是 FUTBIN 的孪生结构，去重只能按 cardId，不能按姓名/slug。
- 译名追加 25 条（`cesar-costa`/`georges`/`cole`/`gomez` 等以 FC26 英雄台账 `heroes/data/prices/fc26/base-heroes.json` 既有 nameZh 为准）→ 英雄中文名 99/99；注入用 `apply-market-name-zh.mjs 2026-09-21 --file .../base-heroes.json`。
- 产物：`daily/2026-09-21.json`（131 张、有效价 118、Console 116/PC 79、区间 131/131、basis `partial-live`）、`reports/daily/2026-09-21/icons-heroes.html`（410,266 B、SHA-256 `58952fd0…`，传奇 131 台账/快照 6 天、英雄 99、FC26 参考 93）。头像传奇 131/131、英雄 95/99；backfill 两轮 +38 键/+19 图，余 1 个 noface(20509)。测试 40/40。
- 未渲染 market.html / market-scan.html、未发布、未新增定时任务、未重开详情页取价（当前价按 cardId 读 current.json）。
- **待裁定**：`base-icons.json`/`base-heroes.json` 的 `version` 字段恒为空串，契约要求写 `"Icon"`/`"Hero"`（09-17 起偏差，下游按文件区分版本故无外部症状）。需改采集器 `mk()` 并重跑采集。

## FC·传奇价格区间（每4小时）03:41–03:52（T03，跨日首轮，全绿）
- 五步全跑完、无失败轮：triage `OK`（未自愈）→ 采集 **131/131 首轮即成功**（2m33s、17 批、5 次宿主页重建、补采未触发）→ 研究 → 快照+监控页 → 同时段建议。`latest.json` `collectedAt 2026-09-20T19:43:41.093Z`、`basis partial-live`；序列 `points[]` 第 61 项（`2026-09-21T03`）。
- 建议集 **40 → 41**（进 7 / 出 6，判据链三重证明 0/0）；区间连续 **34 轮持平**；任一平台有效 113（Console 112 / PC 78）。极值：下沿 69,000 × **21 张**、上沿 15,000,000 × **71 张**，越界 0。`current.json` 2827 条 / 2,353,268 B / 三副本 sha 全等 `802ce933…`。
- 同时段建议：**开服第 4 天（2026-09-21 vs FC26 2025-09-21）**，传奇 131 张 买入 0 / 分批 11 / 观望 77 / 谨慎 13 / 不足 30；英雄 99 张 买入 0 / 分批 20 / 观望 46 / 谨慎 3 / 不足 30。
- **本轮三个新发现**：① `fc26bridge` 的 `PAIRS` 里 `[21526,'carlos-alberto-torres','torres']` 的旧键 `torres` **在 FC26 不存在**（只有 `carlos-alberto-torres`/`torres-sanz`）⇒ 可桥回基数实为 **13 而非 14**，`21526` 属新 slug 直连命中；桥回 13/13 成功、新增命中 5 ⇒ 修正建议集 **46**。② 监控页 FC26 参考区锚点必须用 `indexOf('七、FC26')`（337,396），用 `indexOf('FC26')` 会命中左侧导航（8,390）而把整页 226 张 `pimg` 误报为跨代红线违规。③ 布兰科 `21880` Console 由 889,000 跳到 **15,000,000（= 区间上沿）**，已用单卡探针（宿主页+页内 fetch）复核为**源页真实值**（页面自带 `Trend: 1,514.64% (+14.07M)`），非取数缺陷；同形态共 3 张（21779 / 21806 / 21880）。英雄侧不再整片「数据不足」（`base-heroes.json` 已含 FC26 同期价）。
- 快照校验和冲突连续第 30 轮（每日 `state.json` `58952fd0…`/410,266 B vs 本轮重渲染 `e8feffce…`/410,124 B），属契约第 4 步既有行为，勿回滚。未发布、未调用 run-state、未渲染 market.html。
- 校验脚本 `automation/runs/2026-09-21/icons-heroes/work/verify-t03.mjs`（失败项 0）+ 报告 `verify-t03-report.txt`；明细 `detail-t03.mjs`；探针 `probe-pricebox-t03.mjs`。
- **教训（写脚本必读）**：自建校验脚本的 `chk(name, bad, detail)` 第二个参数必须是「失败谓词」。本轮把 4 条正向断言（`includes(...)`）直接传进 `bad` 位，把「研究页有 partial-live / 无 2026-09-25 / 有免责 / 无外链」四项**正确行为打成 ✗**。已同步进 `fc-icons-hourly-verify` 技能。

## 03:47 market-hourly T03 轮（自动化）— 成功
- 采集 250+500 张（Console 198 / PC 181 有效价）；current.json 2827 卡 generatedAt 19:48:17Z，传奇 131 条保留；watch 240/uv 58/trend 154；Top3 戈登(83) 94×2、班达 92。译名补 arthur-atta=亚瑟·阿塔。重合并+发布成功，线上 generatedAt 一致（retryCount=0）。
## 06:00 FC·足球日报（自动化 d39303af）
- 状态 partial（success 因 evidence.missing 非空自动降级），runId 21698108，06:16 提交。
- 三榜 9 联赛块全覆盖 + 新闻 14 条；verify-football-boards exit 0；产物 reports/daily/2026-09-21/football.html（46680 字节）。
- 新教训：finish 会拒收 openedAt 为空/早于 startedAt 的 sources；一次性子代理无法用 SendMessage 恢复，主流程须在 finish 前本地预校验 evidence，缺 openedAt 时经 CDP 重开来源页补真实时间戳（同时完成复核）。

## 06:17 FC·汇总发布（自动化 269f8c09）— 成功
- `verify-football-boards.mjs 2026-09-21` exit 0 → `coordinate.mjs` merge=success、failedPanels=0、五模块全 partial → workbuddy_sites_deploy（updateExistingApp + entryHtml=index.html + language=static）→ verify-publication exit 0 四检全过，本地=线上 sha `6f7154fa…` 2,974,894 B，链接不变 https://fc27-site.app.workbuddy.host/，无边缘缓存滞后。
- current.json 2,827 卡随目录部署，仅复制未二次分析。publish-status-2026-09-21.json reason 已改为明确记录实际发布路径。

## 07:47 FC·传奇价格区间（每4小时，自动化 7123b0fe）T07 — 成功
- 五步固定流程全绿：triage `OK`（未自愈）→ 采集 131/131 首轮成功（2m31s、17 批、5 次宿主页重建、补采未触发）→ 研究 → 快照+监控页 → 同时段建议。
- 序列 `series/icons.json` `points[]` 第 62 项（`2026-09-21T07` / `partial-live`）；当前价 110 张有效（Console 107 / PC 78）；区间连续 **35** 轮持平（下沿 69,000×21 / 上沿 15,000,000×71，与前 7 轮完全同集）。
- 研究建议 41 → **41**（进 5 出 5，全为真实价驱动）；最临界 = 巴雷西 21524 余量比 1.0029。
- 同时段（开服第 4 天，2026-09-21 vs FC26 2025-09-21）：传奇 买入 0 / 分批 11 / 观望 75 / 谨慎 13 / 不足 32；英雄 买入 0 / 分批 20 / 观望 46 / 谨慎 3 / 不足 30。
- 产物校验 36 条断言全绿；三副本 sha 全等 `80058e6cf9fd956a…`（2,354,543 B）；红线全绿（未发布 / 未 run-state / 未渲染 market.html）。
- 已知缺陷仍存（勿单方面改脚本，等用户裁定）：上游 slug 换名致 `noFc26`=26（修正值 13）、桥回 13/13 修正建议集 **46**；台账 `nameZh` 历史误标 7 张。
- 新教训：`detail-tXX.mjs`（源自 detail-t03）「改动面」段在 ESM 下用 `require('node:child_process')` 会抛 `ReferenceError`，只丢该段与红线段（已在本轮修复并写入技能）。单支 `verify-t03.mjs` 是更优继承基座（36 断言、只改 3 常量）。

## 07:54 市场每小时任务（T07）— 成功
- FUTBIN 热门榜 250 张（Console 195 / PC 172 有效价）+ 进化榜 500 张热度，一次尝试成功；current.json 2,830 张（generatedAt 07:54:46），传奇 131 条 priceRange 幂等保留。
- 关注列表 watch 269；Top：安东尼·戈登(83) 93×2、长谷川唯(88) 90。译名补 6 条后 140/140 命中，词库 1884→1890。
- 渲染 watch/scan → 重合并 → 发布 verified，线上 current.json 200 且 generatedAt 一致；publish-hourly.json 记 T07。

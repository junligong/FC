# 2026-09-16 工作日志

## 03:00 FC·进化专栏（自动化 f366055d）— failed
- runId `643732d2-e91b-4417-9c44-a370768e9151`，03:00:22 启动 → 03:02:45 提交 `failed`。
- 结果：0 张进化卡。**原因非「页面无数据」，而是浏览器扩展通道不可用**，未打开任何来源页。
- 已确证 5 项探针证据（默认 profile 的 WorkBuddy 扩展缺 native messaging 宿主注册；dumate relay `connected=false` 且缺 `DUMATE_HOST_URL`；19222 为独立 profile 实例被规则排除；18488 路由全 404）。
- 处置符合契约：未回退 IAB、未新建 profile、未用 FC26/旧日期数据填充。
- 产物：
  - `automation/runs/2026-09-16/evolution/evolution.json`（status failed，evolutions 空）
  - `automation/runs/2026-09-16/evolution/evidence.json`（5 项实测证据）
  - `reports/daily/2026-09-16/evolution.html`（FAILED 徽标 + 0 张 + 本轮说明/缺失项）
- 后续：需用户把 Chrome 扩展与 WorkBuddy 桌面端接通，否则 news / market 等采集任务同样会失败。

## 03:04 FC·资讯采集（自动化 30296b95）— failed
- runId `db98e026-c514-4ed7-8b2a-c59f9985999a`，03:04:04 启动 → 03:05:03 提交 `failed`。
- 结果：0 条推文。同进化专栏，浏览器扩展通道不可用，`init --mode extension` 返回 `RelayUnreachable`（缺 `DUMATE_HOST_URL`），未打开任何来源。
- 证据：`automation/runs/2026-09-16/news/evidence.json`（4 项探针原始返回记录于 notes）。
- 产物：`reports/daily/2026-09-16/news.html`（FAILED 徽标 + 0 条 + 本轮说明/缺失项记录）。
- 处置符合契约：未回退 IAB、未新建 profile、未用历史日期数据填充。

## 03:09 FC·足球日报（自动化 d39303af）— failed
- runId `e3b7351d-205c-458b-81d0-b9f313d44547`，03:08:22 启动 → 03:09:42 提交 `failed`。
- 结果：三大榜单（积分/射手/助攻）与资讯均为 0。同其余采集任务，浏览器扩展通道不可用（4 项探针实测：init 返回 `RelayUnreachable` 缺 `DUMATE_HOST_URL`；relay 未连通；缺 `com.workbuddy.extension.json` 原生宿主；日常 Chrome 无远程调试端口）。
- 三榜校验闸门 `node automation/verify-football-boards.mjs 2026-09-16` 退出码 1（R6 无射手/助攻数据），故不得提交 success，按契约提交 `failed`。
- 证据：`automation/runs/2026-09-16/football/evidence.json`；产物：`reports/daily/2026-09-16/football.html`（FAILED 徽标 + 三榜空状态 + 本轮说明/缺失项）。
- 处置符合契约：未回退 IAB、未新建 profile、未用旧日期/FC26 数据填充、未做「缺数据回退英超」静默兜底。

## 03:10 FC·汇总发布（自动化 269f8c09）— skipped
- `node automation/coordinate.mjs 2026-09-16` → `merge=no_current_snapshot`、`publish=skipped`。
- 原因：四模块（football/news/market/evolution）本轮全部 failed（浏览器扩展通道不可用），无 success/partial 快照可合并。
- 处置符合契约：不发布、保留线上旧版本（2026-09-15，未下线、未换入口）。
- 复核：`verify-publication.mjs 2026-09-16` 退出码 1；线上 sha256=3f4a95d1… 与本地 daily-merged/index.html 逐字节一致（旧版 09-15 完好），无 09-16 归档链接。
- 记录：`automation/publish-status-2026-09-16.json`（published=false）。

## 12:40 手动发布（用户指定：仅发布）
- 背景：用户当日重写了契约（根 AGENTS.md 改走 WorkBuddy 会话 Chrome 插件；daily.md 要求「全部失败也要发布当日状态页」），并自行重建了 `daily-merged/`（index.html 12:35，标题 `FC27情报台 · 2026-09-16`，392,948 B，含 archive/2026-09-16.html）。
- 执行：仅调用 WorkBuddy 站点发布（updateExistingApp），未采集、未跑 coordinate、未检查浏览器、未改动任何站点文件。
- 结果：发布成功，固定链接 `https://fc27-site.app.workbuddy.host/` 不变；`verify-publication.mjs 2026-09-16` 退出码 0，四检全过（matchesLocal/hasArchiveLink/hasDateAnchor/completeHtml）。
- 哈希：index `cf29de7c…b3f6`（本地=线上，392,948 B）；archive/2026-09-16.html `cd045855…9606`（本地=线上，HTTP 200，392,906 B）。
- 注：`automation/publish-status-2026-09-16.json` 由核验脚本重写为 published=true。

## 15:00–15:20 全量重跑（用户要求「全部执行最新任务」）
- **浏览器通道已切换并可用**：根 AGENTS.md 已改为「唯一通道 = web-access 技能（CDP Proxy:3456 直连日常 Chrome）」，extension/Chrome 插件模式明确废弃。`check-deps.mjs` 退出码 0（Chrome port 9222, proxy ready）。
- 四模块同日重跑（`run-state begin --rerun`，旧 attempt 归档），**全部提交 `partial`（无一 failed）**：
  - football `b00094d9`：积分榜 9 联赛齐（英超/西甲/意甲/德甲/法甲/MLS 东+西/沙特/欧冠），射手榜 8 联赛前 10，助攻榜 0（ESPN assists 视图无表格、fbref 反爬）；新闻未逐条核验。`verify-football-boards` 退出码 0。
  - news `ed96664c`：132 卡片、图片 128/133、视频 11、卡片 4；6 账号未渲染出落窗推文、5 图未落盘。
  - market `5404a66a`：market.html / market-scan.html / market-icons.html 三产物；分层仅 1–10 万档 50 张（≥10 万三档筛选页实测 0 行）、传奇 131 张有效价 97、进化 50、扫描热门进化 60+价值卡 60；活动卡与周黑未采集。
  - evolution `508e769b`：473 张进化卡（去重后）、路线 5 条。
- **合并首次失败**：`coordinate.mjs` 拷写 `archive/2026-09-15.html` 时报 `EIO: i/o error`（磁盘充足 297 GB，属瞬时 IO 故障），导致该归档文件被抹掉。**重试即成功**（`--rerun`）→ `merge: success`、`failedPanels: 0`。
- 发布：WorkBuddy 站点（updateExistingApp），链接 `https://fc27-site.app.workbuddy.host/` 不变；`verify-publication.mjs 2026-09-16` 退出码 0，四检全过。
- 线上首页：`FC27情报台 · 2026-09-16`，SHA-256 `25d089989b2cd20497adbbeb39a135d612eb873244fbe4636a417b9c23f7fa39`，26,379,720 B（本地=线上）；archive/2026-09-16.html 存在。导航 7 入口完整、市场 4 子标签齐全、无「采集失败」面板。
- 经验：`coordinate.mjs` 的 archive 同步是非原子的逐文件 `copyFileSync`，单文件失败会让 loop 中断并留下缺失/陈旧归档 —— 遇到 EIO 重试即恢复；47 MB 级归档页是风险点。

## 10:12 项目知识库入库（ima 个人版）— 成功 2/2
- 需求：把当前项目写入知识库。已用 ima 连接器（个人账号）写入可写知识库「朝阳吴彦祖的知识库」（id `001aaa7e88002f6a`）。
- 生成两份文档并入库（media_state=2，解析 100%）：
  - `FC项目总览.md`（13567 B）：定位、目录职责、五任务链路、产物、站点信息架构、数据边界、已知风险、发布状态。
  - `FC运行手册.md`（9698 B）：执行契约、run-state 状态机、证据规范、校验闸门与测试、合并发布流程、同日重跑、故障排查、命令速查、关键文件索引。
- 本地留档：`.workbuddy/exports/`（含可复用脚本 `upload_to_cos.py`）。
- 关键实现细节：ima 本地文件入库为三步 `create_media → COS PUT → add_knowledge`；COS 签名 v5 的 HttpString 中 header 值必须整体百分号编码（`text%2Fmarkdown`），否则 `SignatureDoesNotMatch`；临时凭证用完即删，不落项目目录。

## 10:20-10:35 项目数据全量同步到 ima 知识库 — 成功 43 条
- 用户选择范围「文档+报告+研究数据」、形态「保留原始文件」。最终知识库 total_size=43（含库内原有 1 份 PDF 使用指南），本次新增 43 条。
- 入库清单：2 份自撰文档（项目总览 / 运行手册）+ 3 份 MD 合集（覆盖 33 个原始 md）+ 2026-09-11~09-16 日报与历史归档 HTML + FC27 投资分析报告 + 10 份研究 CSV + 09-15 归档精简版。
- 范围收敛（已向用户说明）：跳过 2 个与归档同内容的大文件（各 47.7MB）、6 个 933B 空壳归档页；JSON/.mjs 因格式不支持未纳入本批。
- 新发现约束：**ima HTML 单文件上限 10MB**（code 220001）；09-15 归档 47.7MB 被拒，改用「去除 150 处 base64 内嵌图片」的精简版（1.24MB）入库。
- 执行方式：COS 上传器 + 5 个并行子代理分片处理 33 条，全部 uploaded/failed=0，逐条 get_knowledge_list 核验 media_state=2。
- 复用资产：`.workbuddy/exports/{sync_upload.py,build_md_bundles.py,build_sync_manifest.py,build_batches.py,build_chunks.py,build_light_archive.py}`。

## 10:30-10:45 FC26 首月卡价实证 → FC27 开服投资建议（用户点名任务）
- 需求：分析 FC26 开服一个月卡价数据，给出 FC27 投资建议。**未走采集通道**（浏览器通道故障），全部基于项目内已有离线数据集，因此不受通道问题影响。
- 数据底子（`apps/market/engine/`）：`gold/data/prices/fc26/` 152 张金卡 × 30 天 Cross 日频价格（采集完整率 152/152，零缺失）+ `data/analysis/fc27-top200/fc27-price-matrix.json`（227 人 FC26→FC27 OVR/金特技/六维对照）+ `gold/data/prices/easysbc/fc27-gold-price[s].json` 预测价。
- 核心结论：首月**极端分层**——开盘 <1 万档中位 +26.4%（胜率 68%，2–5 千档 +63.5%）、1–10 万档 -51.0%、10–30 万档 -62.3% 且胜率 0%；log(开盘价)↔涨跌幅相关系数 -0.417。峰值时点随档位单调前移（≥100 万档 100% 首周见顶，中位即开服当天；<1 万档仅 30.3%，中位推迟到开服后第 22 天）。OVR 变化↔预测价倍率秩相关 0.77（+2 及以上中位 3.06×，-3 仅 0.08×）；金特技 +1 → 1.69×、-1 → 0.42×。最优组合：低价档开服第 7 天买入、第 21 天卖出，税后中位 +46.7%；中价档整张时点矩阵全为负。
- FC27 增量变量（外部检索）：Web App 9/17 01:00（北京）开启、终极版抢先体验 9/18、全球正式开服 9/25；金卡 QS 或 650→290、最低 BIN 600→300、Common/Rare 合并、SBC 扁平化打分（取消化学/位置要求）、新增 Holographic 稀有度、进化分支且金特技封顶 3 个。
- 产物：`deliverables/trading-agent/fc27-gold-analysis-2026-09-16.html`（自包含，14 章节 / 13 表 / 6 图，深绿主题与站点一致，红涨绿跌）。
- 可复跑流水线（已归档，位置无关）：`automation/runs/2026-09-16/market/work/fc27-investment/{_analyze.py,_prep.py,_build_report.py}` + `_findings.json` / `_report-data.json`，按顺序执行即可重建报告；中间产物只写该工作目录，不污染 `deliverables/`。
- 偏离说明：用户会话所选 Expert 为「交易分析团队」（12 个股票分析 Agent，数据源限定 NeoData 金融数据）。NeoData 不覆盖 FUT 虚拟卡价，故未按该 Expert 的 Agent 群流程执行，改为沿用其分析框架（多维 → 多空论证 → 交易方案 → 三方风险 → Markdown 摘要 + 自包含 HTML）在真实卡价数据上落地，已向用户说明。

## 12:05-12:40 新增「传奇监控」页（市场栏目第三子标签）— 完成
- 需求：新增传奇监控页，监控每日全部传奇卡价格变化，入口放进传奇卡分栏。确认结果：入口=市场栏第三个子标签；数据范围=**仅 FC27 131 张**（不做 FC26 对照表）。
- 新增两份脚本（`apps/market/engine/scripts/`）：
  - `record-icons-daily.mjs`：把当日 FC27 传奇卡抓取结果固化为 `icons/data/prices/fc27/daily/<DATE>.json`。一天一份、同日重跑只覆盖当天、tmp+rename 原子写入；抓取结果不可用时**非 0 退出且不写快照**。价格 <1000 判为占位非有效价；`date < launchDate(2026-09-25)` → `priceBasis=listing-estimate`，之后为 `market`。
  - `render-market-icons.mjs`：渲染 `reports/daily/D/market-icons.html`（五段：监控口径 / 大盘中位价走势 / 全量台账 / 逐日快照记录 / 来源与缺失项）。台账列含评分、位置、六维、金特技数（title 展开）、价格、日环比、累计涨跌、记录天数、逐卡 SVG sparkline；纯内联 JS 支持表头排序 + 搜索。
- 接线（4 处）：`render-market.mjs` 改为三产物入口；`apps/portal/merge_daily_report.mjs` 的 `marketSubs` 增加 `{id:'icons',label:'传奇监控'}`；`apps/portal/dashboard.mjs` 栏目描述与「市场视图」计数由 2 改 3；`automation/coordinate.mjs` 的 extras 数组加入 `market-icons.html`（隔离 stage 才读得到）。
- 契约同步：根 `AGENTS.md`、`apps/market/AGENTS.md`、`automation/prompts/market.md`（新增「3. market-icons.html」小节 + 产出流程加 recorder 步骤）、`engine/modules/market-segments.json`（登记第三份输出与 dataSources）。
- 测试：`regression.test.mjs` 新增 3 例（第三子标签恰好 3 个；快照同日幂等+占位价不计算涨跌+抓取失败不写快照；开服后日环比/累计涨跌/走势/LIVE MARKET）。全量 17 例通过。
- 口径决策（重要）：FC27 未开服前 FUTBIN 只有列表页占位/估算价（本次 131 张中 97 张通过 ≥1000 校验，34 张为 88~95 占位值）。**占位价口径下页面不计算日环比与累计涨跌**，只做台账与记录进度，避免把估算价当行情。开服后自动切换为真实成交价监控。
- 已知数据瑕疵：`base-icons.json` 的 `currentPrice` 存在解析错位（如贝利抓到 95 而非 60000），已用 <1000 下限过滤，但仍可能有少量误读值，开服后需以真实成交价复核。
- 当日产物：`reports/daily/2026-09-16/market-icons.html`（78KB，131 行台账）；`daily-merged/` 已重建（0.19MB，6 篇历史），index 含 3 个子标签。
- 12:21 用户确认「重新发布」→ 用 `workbuddy_sites_deploy`（appName「FC27每日情报台」、`updateExistingApp:true`、`userAskedToPublish:true`）发布 `daily-merged/`，**shareLink 未变**：https://fc27-site.app.workbuddy.host/ 。`node automation/verify-publication.mjs 2026-09-16` 核验通过：本地与线上 SHA-256 完全一致（ecae9303…a8f6，218852 B），四项检查全 true。

## 12:23-12:30 补漏：把「传奇卡研究」报告接进市场栏目（用户反馈缺漏）
- 反馈：用户指出 `fc27-icon-analysis-2026-09-16.html` 在 `#market` 里找不到。**我最初理解偏差**——用户原话「已经整理了 [dumate 链接]，添加到传奇卡分栏中」指的是要把**那份分析报告本体**放进市场栏目，而不只是做监控台账页。
- 修正方案：市场栏目扩到**四个子标签**：市场概览 / 市场扫描 / 传奇监控 / 传奇卡研究。
- 常驻底稿：`apps/market/engine/icons/reports/fc27-icon-analysis.html`（从 `deliverables/trading-agent/fc27-icon-analysis-2026-09-16.html` 复制，60KB 自包含、无外部依赖、深色主题、红涨绿跌与站点一致）。**不是每日产物**：合并器优先取 `reports/daily/D/market-icons-research.html`，缺失则回退底稿。
- 关键改动：`apps/portal/merge_daily_report.mjs` 抽出 `themedPanel()` 并新增 `buildIconResearchPanel()`——常驻底稿**不做当日日期校验**（否则次日合并会静默丢弃这份研究）；`automation/coordinate.mjs` 额外把 `icons/reports/` 拷进隔离 stage（否则日任务的合并路径里研究标签会消失）；`dashboard.mjs` 描述与「市场视图」计数 3→4；`render-market-icons.mjs` 监控页加一行指向研究标签的交叉引用；契约文档与 `market-segments.json`（新增 `iconResearch` 节点）同步。
- 测试：新增「传奇卡研究常驻底稿跨日期并入市场栏目」（底稿内嵌日期与合并日期不同仍须被采纳），全量 18 例通过。
- 已重新发布并核验：本地=线上 SHA-256 `17356b69…90d4`，304760 B；线上抽查 4 个子标签、研究六章齐全。
- **平台缓存坑（重要）**：发布后根路径 `https://…/` 出现**多版本边缘缓存轮询**（12 次采样得到 47.7MB 旧版 ×7 / 218KB 中间版 ×4 / 304KB 最新版 ×1），而 `https://…/index.html` 显式路径 6/6 命中最新版，`eo-cache-status: MISS` 且 origin 正确。约 3–4 分钟后收敛（根路径 12/12 最新版）。**结论：发布后 verify 失败先别急着改代码，先显式路径复查 + 隔几分钟重采样根路径**。

## 12:33-12:45 新增左侧导航「传奇专栏」（第七个入口）— 完成，待发布
- 需求：`fc27-icon-analysis-2026-09-16.html` 已总结传奇卡情况，在**左侧导航**加一个独立「传奇专栏」入口，放入 FC27 vs FC26 传奇卡对比与投资预测报告。
- 定位：与「进化专栏」同构的**独立一级栏目**（不是市场栏子标签）。左侧导航由 6 项变 7 项：今日总览 / 足球动态 / FC27 资讯 / FC27 市场 / 进化专栏 / **传奇专栏** / 历史日报。
- 实现（只动两个文件，接线点比想象中少）：
  - `apps/portal/dashboard.mjs`：新增 `ICONS.legend`（皇冠 SVG）+ `LEGEND_TAB = {id:'legend-column', view:'legend', label:'传奇专栏', en:'ICONS'}`；并入 `navItems`、`CARD_ITEMS`（首页卡片 4→5）、新增 `legendView`；右侧栏目加紧凑 `legend-rail` 卡（状态 + 131/122 + 三条内容要点 + 「进入传奇专栏」按钮），并把 `.rail-cta` / `.legend-rail` 补进 CSS。
  - `apps/portal/merge_daily_report.mjs`：新增 `LEGEND_SOURCE`，`generateDaily()` 里复用 `buildIconResearchPanel(D)` 的结果同时写入 `panels['legend-column']`——**同一份底稿供两个位置**（市场栏「传奇卡研究」子标签 + 左侧传奇专栏）。
  - `automation/coordinate.mjs` **无需改动**：它已整体 `cpSync` 了 `apps/market/engine/icons/reports/` 进隔离 stage。
- 内容源与口径不变：`apps/market/engine/icons/reports/fc27-icon-analysis.html`（72.5KB，六章：双版本阵容对照 / 属性与金特技变化 / FC26 首月基准 / FC27 全量 131 张预测 / 投资分档 / 数据口径），跨日期常驻、不受当日日期校验、缺稿显示如实空状态。
- 契约同步：根 `AGENTS.md` 补一条「传奇专栏」栏目定义 + 修改点清单。
- 校验：全量 18 例测试通过；`daily-merged/index.html` 0.37MB，6 篇历史；左栏文本顺序、`id="view-legend"`、报告正文（标题「FC27 vs FC26 传奇卡对比 · FC27 价格预测与投资建议」）均已核对存在。
- 未决/待用户确认：市场栏「传奇卡研究」子标签**保留未删**（用户原话是「添加」），现同一份报告在站点上有两个入口；如需收敛为一个，告知即可。
- **工具坑（重要）**：同一轮 response 里对**同一个文件**发多个 Edit 会写入竞争，部分改动会静默丢失（本次 dashboard.mjs 丢了 2 处）。改同一文件必须**逐条串行**编辑，并在每批后用 Grep 工具复核落盘（Bash 里的 `grep` 在本次会话多次返回假空结果，不可信）。

## 12:51-12:53 evolution 单任务重跑（runId a0101bcd）— status failed
- 触发：本任务为手动/重跑触发（非 03:00 调度）。`run-state begin evolution 2026-09-16` 返回 accepted=true（同日重跑，上一轮 03:00 的 attempts 已归档）。
- 结果：**0 张进化卡，failed**，12:53:06 提交（远早于 13:06:33 期限）。
- 本轮首次按新契约走 **web-access（CDP）技能**，得到与 dumate 路线不同的、可一步修复的失败原因：
  1. `check-deps.mjs` → exit 1「没有任何浏览器打开远程调试开关」；
  2. 四个 profile 的 `DevToolsActivePort` 全不存在；兜底端口 9222/9229/9333 全关；`127.0.0.1:3456/health` 无响应；
  3. 已执行技能允许的唯一自动补救 `open -a "Google Chrome" chrome://inspect/#remote-debugging` 并复跑，结果不变（勾选必须人工完成）；
  4. 诊断：`curl futbin.com/27/popular/evolutions` → **403 / 641 B**，静态抓取路线不存在，无法替代浏览器。
- 产物：`automation/runs/2026-09-16/evolution/{evolution.json,evidence.json,report.html}` + `reports/daily/2026-09-16/evolution.html`（FAILED 空状态，6090 B，快照 SHA-256 `4f2cb9d3…`）。未回退 IAB / 未新建 profile / 未用旧数据填充；历史日期报告未覆盖。
- 已把「web-access CDP 通道的一步修复方法 + FUTBIN 静态 403」写入 `MEMORY.md` 浏览器通道章节。

## 13:15-13:30 浏览器通道故障定位与修复 — 已修复并实测通过

### 用户报障
1. Chrome 里装了 WorkBuddy 插件，但「连接不上」；
2. 5 个定时任务（football / news / market / evolution / 汇总发布）今日全部 failed。

### 故障一：WorkBuddy Chrome 插件连不上 —— 属于产品侧缺口，用户侧无法修复
- 插件：Chrome Web Store `ajnnogdfpilbhkeggdjlcokgglijmdde`「WorkBuddy」v0.2.3，源码注释显示来自 `@genie/workbuddy-browser-extension`。
- **它的传输通道只有一条**：`background.js` 里 `chrome.runtime.connectNative("com.workbuddy.extension")`，全文件无 WebSocket / HTTP 备用通道。`HOST_NAME = "com.workbuddy.extension"`，重连走 alarm `browser-mcp-extension-reconnect`（每 1 分钟）。
- **本机没有任何 `com.workbuddy.extension` 的 native messaging host**：
  - 三个浏览器的 `NativeMessagingHosts/` 目录里只有 `com.openai.codexextension.json`（ChatGPT 的），无 WorkBuddy 的；
  - 逐字节扫描 `/Applications/WorkBuddy.app/Contents/Resources/app.asar`（298 MB，用 node 分块扫，`grep` 会 OOM）结果：`connectNative` / `NativeMessaging` / `DevToolsActivePort` / `ajnnogdfpilbhkeggdjlcokgglijmdde` / `com.workbuddy.extension` **命中数全部为 0**；
  - `/Applications`、`~/.workbuddy/{plugins,connectors,skills}` 同样 0 命中；agent-browser 插件只是 Vercel 的独立 CLI，与它无关。
- 结论：**WorkBuddy 桌面端 v5.5.6 根本没有实现该扩展的 native messaging 宿主**，所以插件永远是「未连接」——勾什么开关都没用。这不是用户配置问题，不应再花时间尝试修复。
- 顺带发现：桌面端自身支持 `WORKBUDDY_REMOTE_DEBUGGING_PORT` 环境变量开启它内嵌 Chromium 的 CDP 端口（供「super-workbuddy skill 等外部自动化工具」连接），默认关闭；与本插件无关。

### 故障二：5 个任务全部失败 —— 单一根因：浏览器通道不可用（现已修复）
- 4 个采集任务的 `evidence.json` 一致：Web Access 所需的「用户日常 Chrome 远程调试开关」未开启，`check-deps.mjs` exit 1，采集第一步即中止（03:00-03:10 各提交 failed，内容为如实空状态，未用旧数据填充）。
- 第 5 个「汇总发布」是**级联失败**，非独立故障：`automation/coordinate.mjs` 里 `available` 统计 `['football','news','market']` 中有 `snapshotPath` 的模块，本轮三个采集任务都没提交快照（报 failed 时未写 snapshotPath）→ `available===0` → `status.merge='no_current_snapshot'`、`status.publish='skipped'`。设计如此：全日采集失败时拒绝合并发布，避免空状态页覆盖站点。
- **修复**：用户在 13:19 于 Chrome 勾选了 `chrome://inspect/#remote-debugging` 的「Allow remote debugging for this browser instance」。
- **实测通过（关键证据）**：
  - `~/Library/Application Support/Google/Chrome/DevToolsActivePort` 出现，内容 `9222\n/devtools/browser/fd53f450-…`；Chrome 主进程 PID 1039 在 `127.0.0.1:9222` LISTEN（用户日常 profile，非独立 profile）。
  - `node ~/.workbuddy/skills/web-access/scripts/check-deps.mjs --browser chrome` → exit 0，`proxy: ready (Chrome)`；`3456/health` 返回 `connected:true, chromePort:9222`。
  - 实际打开 FUTBIN：`/27/players` 标题 `EA FC 27 Players - Ultimate Team Rating | FUTBIN`、1 表 31 行；`/27/popular/evolutions` 标题 `EA FC 27 Popular Evolution Players | FUTBIN`、1389 个进化相关元素、正文 46963 字符。**两个来源均取到真实数据，通道判定可用**（此前是 403/641 B）。测试 tab 已 `/close`，未动用户原有 tab。
- **固化修复（防明早再挂）**：`~/.workbuddy/skills/web-access/config.env` 写入 `WEB_ACCESS_BROWSER=chrome`。此前为空 → `check-deps.mjs` 返回 **exit 2（needs decision）**，无人值守的 03:00 任务会卡在「询问用户偏好」而失败。改后复跑无参数 `check-deps.mjs` → exit 0、`[config.env 偏好]`。
- **开关是否跨重启**：已核实持久化在 `~/Library/Application Support/Google/Chrome/Local State` → `devtools.remote_debugging = {"user-enabled": true}`，**跨 Chrome 重启有效**，无需每次重勾、也无需 `--remote-debugging-port` 起新实例。
- **判定坑**：`curl http://127.0.0.1:9222/json/version` 在开关模式下返回空，**不能**用它判定通道可用；正确判据是 `check-deps.mjs` 退出码 + 实际 `/new` 打开来源页读到正文。

### 落地改动
- `~/.workbuddy/skills/web-access/config.env`：`WEB_ACCESS_BROWSER=` → `chrome`。
- `~/.workbuddy/skills/fc-browser-channel-check/SKILL.md` v2.0.0 → v2.1.0：新增「通道自检与修复」章节（退出码 0/1/2 处置表、人工开关位置、持久化位置与实测事实、`/json/version` 不能作判据）。
- 5 个定时任务配置本身正确（均绑定 Web Access 技能、cwd 正确、03:00/03:05 调度），**无需改动**。
- 本次未补跑今日采集（当日日报仍为 FAILED 空状态页），等待用户确认是否重跑。

## 13:30-13:40 当日 4 个采集任务重跑 + 合并发布 — 全部 partial，已上线

### 重跑必须带 `--rerun`（重要操作知识）
- 上一轮 03:00 的 `owner.json` 会残留 `status:"running"` 锁，**普通 `begin MODULE D` 必定返回 `accepted=false`**（`run-state.mjs` 用 `openSync(owner.json,'wx')` 抢锁）。
- 正确姿势：`node automation/run-state.mjs begin <module> 2026-09-16 --rerun` → 先把上一轮归档到 `automation/runs/D/<module>/attempts/<旧runId>/`（仅 rename，非破坏性），再开新 run。
- 只读排查用 `node automation/run-state.mjs prepare-rerun <module> D`（归档后即退出）；若该模块 `state.status==='running'` 则会拒绝归档。
- **finish 的硬上限是 `startedAt + 20 分钟`**（不是 deadlineAt 的 15 分钟）；超时提交 success/partial 会被「超过提交期限，不接受迟到版本」拒绝。子任务应在第 14 分钟收口转 partial。
- finish 对 success/partial 的强校验：报告 mtime ≥ startedAt、HTML 含当日日期且以 `</html>` 结尾、`evidence.sources` 非空、**每个 `openedAt` ≥ startedAt**、且 `missingItems` 非空时 success 会被自动降级为 partial。

### 本轮结果（4 个子 Agent 并行，约 9 分钟）
| 模块 | status | runId | sources | 产物 |
|---|---|---|---|---|
| football | partial | 5dd8a205 | 30 | football.html 49,689 B；三榜各覆盖 8 个联赛块；`verify-football-boards` 退出码 **0** |
| news | partial | 03987969 | 16 | news.html 48,018 B；**16/16 账号打开成功**，14 个有内容，入库 31 条推文 |
| market | partial | bc1ad0ee | 8 | market.html / market-scan.html / market-icons.html；传奇快照 131 张 / 有效价 97 |
| evolution | partial | 7c887658 | 4 | evolution.html 223,685 B；456 张进化卡（去重后） |

均提交 partial（非 success）：各有如实缺失项（足球伤停 0 条、news 配图只缓存 1 张、market 三档 ≥10 万筛选页实测 0 行、evolution 缺绝对到期日期与升级数值表）。
- 足球子任务顺带修复了模板中「美职联积分榜引用未定义变量」的真实缺陷；改生成器后 18 例回归测试全通过。
- market 子任务首轮 runId 773c9855 因证据时间戳早于实际采集被 finish 拒绝，已按重跑流程归档并留准记录。
- **口径红线执行到位**：FC27 开服前（launchDate 2026-09-25）价格为 `listing-estimate`，全部产物均未计算日环比/涨跌。

### 合并与发布
- `node automation/coordinate.mjs 2026-09-16 --prepare-rerun` → 归档上一轮协调器尝试 → `--rerun` 合并成功（`merge:'success'`、`failedPanels:0`）。
- `daily-merged/index.html` 1,357,132 B，SHA-256 `2df002f6…cbdc`；archive/ 含 12 篇历史日报（2026-09-03 起）。
- 发布走 WorkBuddy 站点能力，**分享链接保持不变**：https://fc27-site.app.workbuddy.host/ （应用「FC27每日情报台」，`updateExistingApp`）。
- `node automation/verify-publication.mjs 2026-09-16` → **exit 0**，本地/线上 SHA-256 逐字节一致，四项检查（matchesLocal/hasArchiveLink/hasDateAnchor/completeHtml）全 true。
- 注意：`coordinator-state.json` 的 `unattendedPublishingVerified` 恒为 false，是 `coordinate.mjs` 的既定设计（该字段只在初始化时写死），**不代表发布失败**，别误判。发布状态权威记录在 `automation/publish-status-D.json`。

### 顺带完成：禁用 WorkBuddy Chrome 插件
用户确认禁用。经 CDP 在 `chrome://extensions` 页调用 `chrome.management.setEnabled('ajnnogdfpilbhkeggdjlcokgglijmdde', false)` 完成（同步回调 + `window.__wb` 取值复核：`enabled:false`）；其余 7 个启用扩展未受影响。查询扩展状态必须用**回调式** `chrome.management.getAll(cb)`，MV3 下把它当同步数组用会抛 Uncaught。

## 13:40-13:50 把故障定位与流程固化进各层约定文档

### 动机
多个 `AGENTS.md` 仍在要求「连接已登录 Chrome **插件** / `extension` 模式」——该通道已被证实在产品侧不存在，是这次 5 任务全失败的直接诱因。若只改根文件，子目录与每日契约仍会给出矛盾指令。

### 改动的 9 个 AGENTS.md（唯一权威：根 AGENTS.md 的「浏览器强制规则」，子文件只做一行引用）
- 根 `AGENTS.md`：重写「浏览器强制规则」为完整章节——CDP 通道定义、**明令废弃 Chrome 插件 / extension 模式**（附 app.asar 命中数为 0 的证据）、`check-deps.mjs` 退出码 0/1/2 处置表、唯一人工开关及其**跨重启持久化位置**、`/json/version` 不能作判据的坑、禁止入口清单、失败处置约定。
- `automation/AGENTS.md`：新增「单次运行生命周期（run-state.mjs）」小节（`--rerun` 语义、锁残留原因、**`startedAt + 20 分钟` 硬上限**、finish 对 success/partial 的强校验项、failed 也报存快照的条件）；`coordinate.mjs` 条目下补**级联规则**（只统计 `['football','news','market']` 中有 snapshotPath 者；全空则 `no_current_snapshot` + `publish=skipped`，是有意设计）、`unattendedPublishingVerified` **恒为 false 不代表失败**、隔离 stage 会拷入常驻内容源。
- `apps/AGENTS.md` / `apps/football` / `apps/news` / `apps/market` / `apps/portal` / `shared` / `daily-merged`：全部替换为新通道表述，并按模块补入实测坑——
  - football：三榜必做 + `verify-football-boards.mjs` 闸门 + 禁「缺数据回退英超」。
  - news：X 强反爬不得用 WebSearch/curl 代替；**已知覆盖度限制**（时间线虚拟化 + 后台节流，单账号仅 3–7 条；配图懒加载）须如实记缺失并提交 partial。
  - market：FUTBIN 静态 403；开服前 `pc_price` 三档筛选返回 0 行是**筛选失效而非无卡**。
  - portal：纯本地合并，**不得跑浏览器自检**（原来只写「不应启动浏览器」，现在明确到 check-deps 也不跑）。
  - daily-merged：发布须显式 `entryHtml:"index.html"` + `updateExistingApp`；**边缘缓存轮询坑**（显式路径先命中，根路径 3–4 分钟收敛）；发布级联前提。

### 每日契约同步（这四处才是子任务真正读的东西）
- `automation/prompts/{football,market,evolution,news}.md` 第 1 步补**两种 accepted=false 的区分**：调度场景立即停止；**仅当用户本轮明确要求同日重跑**时才用 `--rerun`。这是本次 4 个子 Agent 全部卡住的原因（原契约只有「立即停止」一条路）。
- 四个契约第 2 步补 **`finish` 硬上限 `startedAt + 20 分钟`**（返回的 `deadlineAt` 15 分钟只是提示），要求第 14 分钟收口转 partial。
- football / market / evolution 的「浏览器强制规则」段落补入 `check-deps.mjs` 前置自检与 403 说明；news 的采集段落改为 CDP 表述。
- `automation/README.md`：重写「明确的同日重跑」为一步式 `--rerun` 与两步式 `prepare-rerun` 两种等价写法 + 锁语义说明；单项预算补 20 分钟硬上限。
- `apps/news/README.md`：改为 CDP 表述并注明插件已禁用。
- `automation/task-definitions.json` 经检查**本来就是正确的**（指向 promptFile、写的是 Web Access 技能），无需改动。

### 顺带修掉一个真实缺陷：`publish-status-D.json` 残留失败记账
- 现象：同日「先失败、后重跑成功」后，记录同时存在 `published:true` 与 `mergeResult:"no_current_snapshot"` / `publishResult:"skipped"` / `failureCause:"Chrome 插件 extension 通道不可用"` —— 自相矛盾。
- 根因：`verify-publication.mjs` 用 `...previous` 展开旧记录，只清理了 `details`/`suggestedActions`，没清 `failureCause` 等失败记账。
- 修法：抽出纯函数 `buildPublishStatus({previous,verification,date,localIndexHtml,coordinator})`；**验证成功时**丢弃 `failureCause`，并从当日 `coordinator-state.json` 重取 `mergeResult`/`publishResult`/`modules`/`runIds`（新增字段，便于日后溯源到具体 runId）；验证失败时保留失败原因供排查。
- 补 2 例回归测试（现共 20 例全通过），覆盖「成功清除失败记账」与「失败保留失败记账」两个方向。
- 修正后今日记录已是 `published:true / merge:'success' / publish:'delegated' / 四模块 partial / runIds 齐全 / 无 failureCause`，`verify-publication.mjs` 仍 exit 0。

### 5 个定时任务的执行记忆已更正
`~/.workbuddy/.../memory/automations/<id>/memory.md` 共 5 份，原先都写着「待/需用户侧修复：接通 Chrome 扩展（注册 native messaging 宿主）」。逐份追加 13:40 更正段：该结论作废（产品侧缺口）、现行通道与自检方法、`--rerun` 与 20 分钟时限，并补入本模块 13:30 重跑的真实结果。**不更正会让后续运行继续按错误前提排查。**

### 派生导出已重建
`.workbuddy/exports/sync/FC项目约定与任务契约合集.md` 等 3 份合集由 `build_md_bundles.py` 从上述源文件拼接生成，已重新生成（合集 63,260 B / 17 sources），确保 ima 知识库条目与新约定一致。**尚未重新上传到 ima**（该步需连接器 + COS 上传，未在本轮执行）。

### 遗留
- `.workbuddy/exports/sync/` 3 份合集已更新但未同步到 ima 知识库，需要时可跑 `sync_upload.py`。
- `automation/runs/2026-09-16/**/attempts/` 内保留了当日失败轮次的原始证据（含旧的 extension 探针记录）——这是不可变历史留证，**不应清理**。

## 14:00-14:30 X 配图/视频截取问题定位与修复

### 用户报障
「很多带视频或者带图片的都截取不到」。

### 证据链（全部实测）
1. **视频推文被整条丢弃**：旧脚本 `apps/news/auto_news.sh:122-123` 有 `var videoEl = article.querySelector("video, [data-testid=videoPlayer], [data-testid=videoComponent], [data-testid=AnimatedGIF]"); if (videoEl) continue;`。核对 8 天全部数据：**506 张图片 100% 是 `pbs.twimg.com/media/`，视频/动图缩略图命中数为 0**，与该 `continue` 完全吻合。
2. **X 在后台标签页把推文媒体渲染成骨架占位符**：实测打开 5 图推文固定链接，`document.querySelectorAll('img').length === 0`，`[data-testid="tweetPhoto"]` 6 个但内部无 `<img>`（只有一个 `filter: blur` 的占位 div）。执行一次 `Page.captureScreenshot` 强制出帧后 `img` 0→6。**所以 DOM 抠图既不可靠也不完整**。
3. **视频海报其实可拿到**：`video[poster]` 与 `[data-testid="videoComponent"] img` 都是 `https://pbs.twimg.com/media/…jpg`，但流程完全忽略 `video` 字段。
4. **链接卡片在后台标签页根本不出图**：`card.layoutSmall.media` 存在但无 `<img>`、无 href，等 20 秒也不出；`player_image_original` 只在接口里。
5. **管道只放行 `/media/`**：`shared/lib/report-assets.mjs:11` 与 `generate_report.mjs` 的两处正则都是 `^https://pbs\.twimg\.com/media/`，视频缩略图（`amplify_video_thumb`）与卡片缩略图（`card_img`）永远进不了缓存。
6. **后台标签页时间线被硬卡在 5 条**：`document.visibilityState === "hidden"`，滚动到 `scrollY 8006 + clientHeight 912 = scrollHeight 8918` 到底后 `scrollHeight` 不再增长、无 spinner、无更多推文。这不是本次回归（09-15 也只有 ~7 条/账号）。

### 解决方案：媒体改走 X 公开 syndication 接口
`https://cdn.syndication.twimg.com/tweet-result?id=<推文ID>&token=a`（需常规 UA，无需登录态），实测 HTTP 200，返回：
- `mediaDetails[]`：`type` = photo / video / animated_gif；`media_url_https`（视频即首帧海报）；`video_info.variants[]`（mp4 直链 + 时长）
- `photos[]`（兜底）、`card.binding_values`（`player_image_original` 等 IMAGE 键 + `title`/`description`/`player_url`）
- `quoted_tweet`（嵌套完整对象）、`created_at`、`text`、`user`

**分辨率对照（实测，146/146 请求全部成功）**：
- 2026-09-16（失败轮）：图片 1 张 → **27 张 / 20 条推文**；**恢复 5 条视频**（均有封面 + mp4 直链 + 时长）；引用推文 4 条含图 4 张。
- 2026-09-15（历史「好」轮）：150 → 116 张（**32 条引用推文的 35 张图被正确拆分出去**，总量 116+35≈151 与原来持平，但归属正确）；视频 0 条（当时已被整条丢弃，无法回溯补回）。

### 另一个独立阻塞：图片 CDN 在沙箱内不可达
- 现象：`curl pbs.twimg.com` → `SSL_ERROR_SYSCALL`；`--noproxy` → `Connection reset by peer`。环境里有 `HTTPS_PROXY=http://127.0.0.1:63882`（工作区出口代理），但两种路径都不通；`nc -z` 能连上 TCP，说明是 TLS 层被干预。
- 而 `cdn.syndication.twimg.com` 走同一代理是通的（HTTP 200），说明是 host 级差异。
- **用户浏览器能访问该 CDN**：在 `https://x.com` 页面内 `fetch()` 图片 → HTTP 200 / 45074 B / `image/jpeg`；CORS 只放行 `x.com` 来源，`about:blank` 空页取不到（返回空对象）。
- 因此新增 `apps/news/fetch-images-browser.mjs`：**curl 失败项自动经 CDP Proxy 在 x.com 页面内 fetch → base64 → 落盘**；优先复用已有 x.com 标签页，没有才开 `https://x.com/robots.txt`（轻量、不加载完整应用）并用完关闭。
- 效果：隔离环境重跑，**图片落盘 0/32 → 32/32**。

### 落地改动
- 新增 `apps/news/x-media.mjs`（解析库：`normalizeTweetMedia` / `normalizeCard` / `collectTweetImageUrls` / `fetchTweetMedia` / `applyMediaToTweet`）。
- 新增 `apps/news/enrich-tweet-media.mjs`（CLI：给当日快照补齐媒体，原地原子更新，逐条失败写 `mediaResolved:false`）。
- 新增 `apps/news/extract-timeline.js`（确定性 DOM 抽取：只取 ID/链接/作者/时间/正文 + `hasVideo/hasPhoto/hasCard` 标记，**不再丢弃视频推文**）。
- 新增 `apps/news/fetch-images-browser.mjs`（浏览器兜底下载，含 URL 注入安全测试）。
- 改 `shared/lib/report-assets.mjs`：新增 `isCacheableXImage()` 按路径白名单放行（`/media/`、`/card_img/`、`/amplify_video_thumb/`、`/ext_tw_video_thumb/`）；`originalXImageUrl` 只对 `name=` 有意义的路径改写（视频缩略图尺寸在路径里，改写会 404）。
- 改 `apps/news/generate_report.mjs`：缓存正文配图 + 视频封面 + 卡片缩略图；渲染视频徽标/时长/原推入口、链接卡片、引用推文块；空状态三态化（未解析 / 含媒体未取到地址 / 确实纯文本），**不再笼统写「原推为纯文本，无配图」**。
- 新增 `automation/news-media.test.mjs`（13 例）；测试命令已在根 `AGENTS.md`、`automation/AGENTS.md`、`automation/README.md` 三处补上该文件。全套 **33 例通过**。
- 契约同步：`automation/prompts/news.md`（四步流程、排除规则精化、两级下载）、`apps/news/AGENTS.md`（媒体两层分工、视频不得丢弃、下载两级）。
- 隔离验证：`FC_PROJECT_ROOT=/tmp/fc-news-stage` 跑通 `enrich → generate`，报告 31 卡 / 32 张本地图 / 0 远程热链 / 5 视频徽标 / 4 引用块，体积 55,548 B。

### 待用户确认
`news.md` 原文写「排除预测、纯推广、**视频和 GIF**」，这是业务规则不是 bug。已改为「排除纯预测、纯推广，以及除封面外无信息文本的纯视频/纯 GIF；带信息量的视频推文正常收录」。**如与原意不符需告知回退。**

## 14:05-14:20 把新资讯采集流程接入任务并完善文档

### 定时任务
- `FC·资讯采集`（id `30296b95-7d89-4802-928c-b85c1fb3a3d1`）的提示词已重写：保留「先读根 AGENTS.md，再读 automation/prompts/news.md（唯一完整契约）」的 bootstrap 结构，并明确写出**固定四步**（DOM 抽取 → syndication 补媒体 → 生成报告 → 校验提交）、媒体不得从 DOM 抠取、严禁因含视频/动图丢弃整条推文、媒体未解析不得写「原推为纯文本无配图」。
- `automation/task-definitions.json` 的 news `bootstrapPrompt` 同步补四步摘要（JSON 已校验合法）。

### 文档
- **`apps/news/AGENTS.md` 重写为完整契约**（分七节）：数据流水线（四步表 + 为什么媒体不能从 DOM 取）、媒体接口字段说明、媒体规则（禁丢视频/落盘白名单/两级下载/三态空状态）、覆盖度与失败判定、数据与快照纪律、报告内容要求、相关文件。
- **`apps/news/README.md` 重写**：流程图 + 文件职责表 + 「两个容易踩的坑」（DOM 取图、curl 拉不到图）。
- **`automation/prompts/news.md`**：标题注明 2026-09-16 修订；卡片渲染改为明确的三态规则；末尾新增**提交前自检表**（7 项：日期/卡片数/中文完整性/媒体解析对照/图片落盘无热链/账号覆盖/来源链接），并明确「访问失败、解析失败、落盘失败、翻译失败分别记录，不得合并成一句部分失败」。
- **`automation/prompts/README.md`**：五份提示词从「只有名字」补成一句职责说明，news 那条点明四步与两条禁令。
- **根 `AGENTS.md`**：在足球条目后新增 news 专属条目（两层取媒体、禁丢视频、两级下载、三态空状态），并指向 news.md 与 apps/news/AGENTS.md。
- 派生合集已重建（`FC项目约定与任务契约合集.md` 74,075 B / 17 来源）。

### 复验
- 隔离环境（`FC_PROJECT_ROOT=/tmp/fc-news-stage`）重跑完整两级流程：媒体解析 31/31 成功，报告 31 卡、视频 5 条。
- 图片落盘 **30/32**（此前一次是 32/32）。给浏览器兜底加了 3 次退避重试后仍固定失败 2 张，逐项排查确认**非瞬时问题**：`https://pbs.twimg.com/media/HSRzQsgXsAEezGs.jpg`（`x.com/FIFA22_INFO/status/2099930065496674692` 的第 2 张）与 `https://pbs.twimg.com/amplify_video_thumb/2100005972408221698/img/mjQhoU4L8ckrmxEu.jpg`（`x.com/FutSheriff/status/2100005994474455177` 的视频封面）在**任何页面上下文、任何 credentials 模式、以及 `<img>` 直接加载下全部失败**，属 X 侧该媒体不可获取（删除/受限），不是我们的取图方式问题。处置：`generate_report.mjs` 保留远程 URL 兜底并记录未落盘数量，任务据此提交 `partial`。
- 全套测试 33 例通过；五个新脚本 `node --check` 语法均 OK。

### 遗留
- 今日已发布的 `news-2026-09-16` 仍是最早那版（1 张配图），**未改写**：报告文件已被 run-state 提交为不可变快照，直接重生成会造成「快照与报告文件不一致」。若要今天就生效，需走 `--rerun` 重跑（会占用一个新的 20 分钟窗口）后重新合并发布。

## 14:30-14:55 把修复推到线上站点（资讯栏目）

### 目标
用户要求把配图/视频修复更新到 https://fc27-site.app.workbuddy.host/#news 。

### 途中发现并修掉两个新缺陷
1. **索引膨胀 34MB**：合并后单文件站点达到 34,253,820 B。根因是报告图片统一取 `name=orig` 原图（98 张共 23.6MB，单张最大 1.43MB）。实测同一张图 `orig` 1469KB / `large` 539KB / `medium` 238KB / `small` 95KB。
   - 修法：`shared/lib/report-assets.mjs` 的 `originalXImageUrl(value, size='orig')` 与 `reportImageAssetName(value, size='orig')` 新增 size 参数（默认 orig，保持既有调用方行为）；`generate_report.mjs` 用常量 `X_IMAGE_SIZE='medium'` 统一传参。尺寸进哈希，换档位会换资产名，**旧文件必须清理否则成为孤儿资产**。补 1 例测试（现 34 例）。
   - 效果：128 条推文 117 张图 → 资产 18MB、索引 26MB（34MB → 26MB）。
2. **浏览器兜底下载器会整体挂死**：某轮 117 张只成功 2 张，`finish` 因报告未重新生成而被拒。
   - 根因：`ensureHost` 会**复用**已有 x.com 标签页，而这样的页在后台会被 Chrome 冻结（JS 停摆、fetch 永不返回），于是一挂几十秒。同一请求单独测试只要 349ms，说明不是限流、不是 CDN 问题。
   - 修法：`fetch-images-browser.mjs` 改为**始终自建宿主页**（`https://x.com/robots.txt`，并轮询到 `location.origin` 就绪）、**默认串行**（concurrency=1）、失败时**重建宿主**再重试。实测 117/117、74.5 秒、16.4MB。

### 合规重跑（runId a8e77103）
- `begin news 2026-09-16 --rerun`（归档上一轮 01db5b1b）→ 用 `/tmp/sweep.mjs` 脚本按 `sources.txt` 逐个开账号 + 执行 `extract-timeline.js`（16/16 成功，每账号 DOM 渲染 6–7 条，命中 24h 窗口 15 条）→ 合并进快照（116 → 128）→ `enrich-tweet-media.mjs`（128/128 解析成功）→ `generate_report.mjs`（**128/128 图片落盘**）→ 写证据（16 源 / 3 缺失项）→ `finish partial`（223,667 B，sha `641ca243`）。
- 合并：`coordinate --prepare-rerun` + `--rerun` → `merge=success`、`failedPanels=0`、index `e9e0930d…`、26,345,934 B。
- 发布：WorkBuddy 站点能力，`updateExistingApp` + `entryHtml=index.html`，分享链接不变；`verify-publication` **exit 0**，线上与本地 SHA-256 逐字节一致。

### 线上核验（重要：面板用 iframe 承载，别查错文档）
- 合并页把每个栏目放进 **`<iframe class="panel-iframe" srcdoc="…">`**（142 处），卡片在 **iframe 的 contentDocument** 里，不在父文档。
- 父文档里 `document.querySelectorAll('article.tweet-card')` 恒为 0、TreeWalker 也找不到 —— 第一次核查就是这么误判成「栏目空白」的。**正确查法**：取 `iframe.contentDocument` 再统计。
- 视图切换是 `hidden` 属性 + `data-view` + `location.hash` 三件套（脚本内联在页尾），`#news` 有效。
- 实测线上资讯栏目：128 卡片 / **128 张内联图 / 0 远程热链** / 11 视频徽标 / 4 链接卡片 / 40 引用块 / 128 中文块。截图确认图片与「视频 13 秒・在原推中观看」徽标均正常渲染。

### 遗留与如实说明
- **34/128 张卡片仍是「待翻译，请查看原文」**（快照里 34 条无 `translation`）。根因：`apps/news/.api_key` 不存在 → `translateTweet` 无法调用，只能靠执行任务的 AI 手工翻译；`generate_report.mjs` 只对本轮 `newTweets` 翻译，继承来的条目不会补译。**这是既有缺口，非本次引入。**
- 今日 `assets/news/` 的旧 orig 资产（98 个 / 24MB）已清理，避免孤儿文件被打进发布包。
- 运行过程中前面子任务遗留了 3 个孤儿标签页（2 个 futbin / 1 个 x.com），已关闭；用户原有的 3 个百度标签页在 14:15 前后消失，非我可确认的指令所致，已如实告知用户。

## 16:20 市场/传奇英雄三拆重构（用户第 5 轮需求）

**需求**：① 市场监控补 PC 平台 + 加平台切换按钮；② 传奇/英雄内容从市场栏目拎出、独立成「传奇/英雄专栏」（原「传奇专栏」改名）；③ 新建独立任务监控全部传奇卡与英雄卡。

**实机核验（本次改造的事实依据，CDP 直连用户 Chrome）**：
- FUTBIN 只提供 **Console（PS/Xbox 合并）** 与 **PC** 两档；`form.desktop-platform-change-form` 内按钮 `ps:Console` / `pc:PC`；`/27/players` 与 `?rarity=icon`、`?rarity=hero` 每行均含 `td.table-price.platform-ps-only` 与 `td.table-price.platform-pc-only`（各 30/30）；`td.table-item-score` = 估值列（60000），开服前两平台价均为 0。
- `?rarity=icon` 首访命中 Cloudflare「请稍候…」，重试即通。

**代码改造（15 个文件）**：
- `apps/market/engine/modules/market-segments.json`：新增 platforms（console/pc + futbinParam/cellClass）、`platformDefault=console`、删 icons-heroes 段与 market-icons.html output、新增 legendColumn。
- `render-market-overview.mjs`：导出 `PLATFORMS`/`PRICE_TIERS`（改 `rangeParam`）、`priceCell()` 双平台单元格、平台切换条 + CSS + 内联脚本；概览四段式 → **三段式**。
- `render-market-report.mjs`（扫描）：新增平台切换条、双平台价格列（**并修复球员库恒空的缺陷**——`players.json` 不存在时回退 `market.json.players`，367 行恢复）、价格分档与排序跟随当前平台。
- `render-market-icons.mjs`：新增 `ICON_PLATFORMS`/`platformCell()`，价格·日环比·累计涨跌·走势按平台各算一份（`.pv.pv-<plat>`），平台切换条，排序用 `activeText()` 只取当前平台值；页面更名「FC27 传奇/英雄监控」。
- `record-icons-daily.mjs`：逐卡增写 `platforms.console`/`platforms.pc`（兼容 `cross`/`ps` 别名）+ `counts.platformValid`。
- `render-icons-heroes.mjs`（新入口）：递归扫描 heroes/data（原只扫顶层导致 0 张），英雄区块插在 `<div class="footer">` 前（原用 `</footer>` 判断恒为假、区块被追加到 `</html>` 之后）。
- `run-state.mjs`（outputs 加 icons-heroes）、`task-definitions.json`（新任务）、`shared/config/project.json`（新增任务 + browser 段改为 web-access/cdp-proxy）、`coordinate.mjs`（optionalPanels 泛化，extras 去掉 market-icons.html）、`prompts/icons-heroes.md`（新建）、`prompts/market.md` + `apps/market/AGENTS.md` + 根 `AGENTS.md` + `prompts/README.md`（同步契约）。
- `dashboard.mjs`：`LEGEND_TAB.label='传奇/英雄专栏'`，抽出通用 `buildViewBody()` 使 legend 也支持子标签；首页 rail 卡片、hero 副标题同步。
- `merge_daily_report.mjs`：`subPanels.legend`（monitor=icons-heroes.html / research=常驻底稿）；market 只剩 overview/scan。
- `regression.test.mjs`：3 项旧断言改为新契约 + 新增扫描页双平台测试。

**结果**：测试 35/35 通过；market 快照重提（partial，sha `d9e7a5bf…`）、icons-heroes 快照首轮 + 改版重提（partial，sha `a2aa575e…`）；coordinate `merge=success`/`failedPanels=0`；发布 `https://fc27-site.app.workbuddy.host/` 并 `verify-publication` exit 0（index sha `8b9c6b00…`，本地=线上 26.5MB）。
新建定时任务 `FC·传奇英雄监控`（id `1868689f-2406-48be-9ec5-fa1880181daa`，每日 03:00）。

**踩坑（已写入技能 fc-run-state-resnapshot）**：同一文件并行发多个编辑会互相覆盖且回报成功（本次丢了 `const subPanels`、dashboard rail 卡片、两处标题），必须串行 + Grep 复核。

## 16:50-17:00 第 6 轮需求：双平台口径落进定时任务与 AGENTS.md（并修掉两个真实缺陷）

### 需求与核查结论
用户要求：优化定时任务——市场监控与传奇/英雄卡监控都必须考虑 PC 与主机（Console）平台，并写进 `AGENTS.md`。
核查（**用 Grep 工具**，bash `grep` 对含中文的多分支正则失配，曾误判成「AGENTS.md 缺章节」）：需求**已全部落地**，无遗漏 ——
- 根 `AGENTS.md`：已有「FUTBIN 平台口径强制规则」章节（页面结构实测表 + 6 条采集规则）+ 「WorkBuddy 定时任务（6 个）」表；market 条目补 `players[].psPrice`/`pcPrice` 与平台切换说明；icons-heroes 条目补 `platforms.console`/`platforms.pc`。
- 契约文件：`automation/prompts/market.md`、`automation/prompts/icons-heroes.md`、`apps/market/AGENTS.md`、`automation/prompts/README.md`、`automation/task-definitions.json`（两者 bootstrapPrompt）均已含双平台要求。
- WorkBuddy 定时任务 prompt 实测（`automation_update` view）：`FC·市场监控`(b195b907) 与 `FC·传奇英雄监控`(1868689f) 都写明「列表页每行同时渲染 `platform-ps-only`/`platform-pc-only`，一次打开即可读到两平台价，分别落库 `psPrice`/`pcPrice`（逐卡 `platforms.console`/`platforms.pc`）；不要依赖 `ps_price`/`pc_price` URL 参数；`table-item-score` 是估值列」。

### 回归测试揪出两个真实缺陷（同日并行编辑覆盖的第二次踩中）
1. **`render-market-overview.mjs` 丢失 `const PTOTAL` 声明** → `renderOverview()` 抛 `ReferenceError`，市场概览整体渲染失败。若不在次日 03:00 前修掉，market 模块将无法提交快照，进而拖垮汇总发布。已补回 `const PTOTAL = allOverviewItems.length`。
2. **平台提示逻辑误述**：原判据 `oneSided` 为假被当作「两平台均已采集」，但开服前两平台价全为 0 时 `pricedPlatforms` 也是空集 → 页面会输出错误结论。改为**三态提示**：双平台齐备（带有效价计数）/ 单边缺失（`<b class="plat-warn">` 告警）/ 全零（说明是估值口径、不算涨跌）。
新增回归用例「市场概览平台提示三态」（三个隔离临时工程各渲染一次，断言文案与告警标记），**全套 36 例通过**。

### 文档订正
- `README.md` 仍停在重构前（四个任务 / market.html 四段式 / 无传奇英雄专栏 / 测试命令缺 news-media）→ 更新为 6 任务、三段式 + Console·PC 平台切换说明、`icons-heroes.html` 与「传奇/英雄专栏」、测试命令补 `news-media.test.mjs`。
- `render-market-icons.mjs` 头部注释改为「传奇区块组件，由 `render-icons-heroes.mjs` 引入；直接运行写出的 `market-icons.html` 仅供回归测试兼容，已非站点产物」。

### 线上复核与如实说明
- `node automation/verify-publication.mjs 2026-09-16` **exit 0**，四检全过；本地=线上 SHA-256 `8b9c6b002386…`，26,497,051 B。
- 09-16 已发布的市场概览产出于 16:18，与本轮修好的渲染器**仅差平台提示一句话**（数据、三段式版式、平台切换按钮完全一致）。按「快照不可变 + 重提快照需新的 run-state 窗口」约定**未重渲染**，次日 03:00 任务自动使用新逻辑；未做「无谓重渲 + 重新发布」的churn。
- 记忆维护：根 `MEMORY.md` 超注入上限被截断，压缩重写 10,470 B → 6,232 B（10 章并为 9 节），保留双平台口径、浏览器通道、协调规则等承载性事实。

---

## 18:15 新增：当日五栏目汇总 → 抖音竖版速报视频（首条内容生产线）

用户需求：把当日任务汇总的各项信息做成抖音短视频。经确认选：竖版 MP4 / FC27 游戏情报主线 / 情报速报调性。

### 交付物 `deliverables/douyin/2026-09-16/`
- `fc27-daily-2026-09-16.mp4` —— 1080×1920 / 30fps / 37.8s / h264 yuv420p / 1.9MB，8 段式（钩子→时间线→TOTW1与全息卡→周五SBC→进化→避坑→足球速览→互动收尾）
- `cover.jpg`（1.6s 帧）、`发布文案.md`（含合规自检 + 逐条溯源 + 发布建议）、`build_video.py`（渲染器，改文案后重跑约 30 秒）、`preview.py`（8 段版式联排预览）

### 渲染管线（已存 skill `fc-daily-douyin-video`）
Pillow 逐帧绘制 → imageio-ffmpeg 管道编码。专用 venv `/Users/wuyanzu/.workbuddy/binaries/python/envs/fc-video`（Pillow 12.3 + imageio-ffmpeg 7.1，**自带静态 ffmpeg**，无需 brew 安装）。本机系统 Python 无 PIL、也没有 ffmpeg，不能直接跑。
本次实测踩到的坑：
1. **`ImageDraw` 在 RGBA 图上绘制是「替换」不是「混合」** → 半透明填充/文字经 `convert("RGB")` 后变成纯色块。正解：canvas 保持 RGB，用 `ImageDraw.Draw(canvas, "RGBA")` 才会按 alpha 混合。
2. `alpha_composite` 的 alpha 是 **0–255**：早先把 `peak=17` 当作 0–1 的 alpha 传入，被 clamp 成 255 → 光斑变成整块纯色盖住文字。
3. `imageio_ffmpeg.write_frames(..., macro_block_size=1)`，否则 1080 宽被强制对齐到 1088。
4. 可用字体：`/System/Library/Fonts/Hiragino Sans GB.ttc`（index 0=W3 / 2=W6）与 `HelveticaNeue.ttc`（1=Bold / 9=Black）；**本机没有 PingFang.ttc**。
5. 长标题一律过 `fit_font()` 自适应字号，否则溢出卡片或被挤成孤字换行。

### 内容口径
视频内全部数字取自当日 5 个 run-state 产物（news / market / evolution / icons-heroes / football），无推断填充；已按项目口径标注「开服前双平台价均为 0、属 listing-estimate 估算口径、不谈涨跌」，并剔除推文里第三方招揽话术（如"首月可赚 1-3M"）。当日如实空状态（Promo / TOTW 名单、三档价格分层、足球助攻榜）未进视频，另在文案文件里单列。

### 待办
- 专家内置 `douyin-prohibited-word` 依赖 `REDFOX_API_KEY`（当前未配置）→ 本次为本地人工合规审查，**未过官方词库**；配置密钥后可重跑复核。
- 抖音热榜 / 日榜 / 飙升榜技能同因缺 key 未调用，故本轮未做热点对标与发布后效果追踪闭环。

### 18:20 补：首版成片是无声视频，已补齐音轨

用户指出「这个视频里面是没有音频的」——确认属实：`imageio_ffmpeg` 只写视频轨，首版 mp4 仅有 `Stream #0:0 Video`。已补旁白 + 垫乐，产物扩为三版：
`fc27-daily-2026-09-16.mp4`（旁白+垫乐，主交付）/ `-voice-only.mp4` / `-bgm-only.mp4`；新增 `audio_build.py`。

- 旁白走 macOS `say -v Tingting`（zh_CN），逐段合成后按画面段落起止时间对齐，语速 185–225 **自适应**，脚本打印「语音时长 vs 画面段长」对照表自证每段都不溢出（实测 8 段全部落位）。
- **重大发现**：本机 `Eddy`/`Flo`/`Sandy`/`Reed`/`Rocko`/`Shelley`/`Grandma`/`Grandpa` 这 8 个 macOS「新语音」对中文**只输出 2.17 秒残缺音频**（21 字文本），文件字节数几乎一致 → 中文场景不可用，只能用 Tingting / Meijia / Sinji。
- 垫乐用 numpy 正弦叠合（根音+泛音+0.11Hz LFO），无版权素材，压到人声之下；段间 0.5s 淡入淡出交叠。
- 三个 ffmpeg 坑：① 不能读写同一文件 → 先 `-an -c:v copy` 剥出 silent.mp4 再 mux；② 多输入必须 `-map 0:v:0 -map 1:a:0`，否则音频取到第一个输入的轨；③ 响度统一 `loudnorm=I=-14:TP=-1.5`，校验用 volumedetect 但**不能加 `-loglevel error`**（它输出在 info 级）。
- 实测：主版 mean −17.1 dB / max −1.4 dB，无削波，逐段均值 −16~−21 dB（无静音段）。
- 遗留：**TTS 读音未试听验证**（"FC 二十七""C 罗""布阿迪"），已在交付文档里明确提示用户发布前试听，并给出换音色/改词重跑路径。

### 18:30 转向：改用剪映出片（系统 TTS 被反馈「太突兀」；用户剪映有会员）

用户反馈系统配音"太突兀"，并说明剪映已打开且是会员 → 交付形态从"我出成片"改为**"我给剪映备料"**：

- `build_video.py` 的 `OUT` 改为 `fc27-daily-2026-09-16-silent.mp4`（纯画面无音轨），编码提到 `-crf 15 -preset slow`（2.44MB，1080×1920/30fps/37.8s）。
- 新增 `make_srt.py` → `fc27-daily-2026-09-16.srt`：8 条字幕带时间轴，剪映「文本 → 本地字幕」导入后一键「朗读」即可生成 AI 配音。
- 新增 `剪映成片指引.md`：4 步（导入画面 → 导入 SRT → 朗读选音色 → 隐藏文本轨 + 配乐 + 导出 1080P）。
- 口播词按 **AI 配音约 4 字/秒**重新配比，起声留 0.3s、收声留 0.1s；第 1 段刻意压到 8 字（先给画面再起声，抗"突兀"）。实测 8 条预估配音全部短于字幕时段，口播总量 29.5s / 视频 37.8s。
- 关键提醒已写进指引：**朗读后隐藏文本轨而非删除**（删轨可能连带删配音）；**导出选 1080P 最清晰**（素材原生 1080×1920，选 2K/4K 只是放大插值）。
- 系统 TTS 三个版本保留作兜底，未删除。
- 未做：4K（2160×3840）素材重渲 —— 需给 `build_video.py` 引入全局 SCALE 贯穿所有坐标与字号，属结构性改造，用户未确认前不动。

### 技能同步
`fc-daily-douyin-video` 已改为**剪映优先**：第 0 节声音选项、第 9 节（拆成「首选剪映路线」+「兜底系统 TTS」）、第 8 节目录树三处同步，避免下次又先做一版会被否掉的 TTS。

### 18:36 补充：剪映「本地字幕」入口位置（用户卡在首页找不到）

用户截图停在**剪映首页**，那里没有「文本 → 本地字幕」——该菜单只在**草稿编辑器内部**（导入视频进入编辑界面后，顶部工具栏 媒体/音频/文本/贴纸/特效 才出现）。

处置：
- `make_srt.py` 增输出 `口播稿.txt`（8 条按时间顺序 + 建议入点），供「手动逐条新建文本」的兜底路线。
- `剪映成片指引.md` 第 1、2 步改写：明确必须先点「开始创作」进编辑器；给出主路径（顶部「文本」→「本地字幕」）+ 3 条备选（文件→导入→本地字幕 / 把 .srt 拖进编辑器窗口 / 手动逐条建文本）；并提示新版常把入口收进「智能字幕」分组。
- 用 Visualizer 画了 5 步路径图给用户对照。

**可复用结论**：给剪映备料时，指引必须写明「菜单在编辑器内，不在首页」，否则用户极易在首页卡住。

